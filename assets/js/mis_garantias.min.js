import{AVAILABLE_DOCS}from"./modules/docs-config.js";const ADD_DOC_KEY="add-document";(()=>{"use strict";document.addEventListener("DOMContentLoaded",()=>{const tbody=document.querySelector("tbody[data-current-page]");const table=tbody.closest("table");const docsConfigMap=new Map(AVAILABLE_DOCS.map((doc)=>[doc.key,doc]));const RECEIPT_ALLOWED_MIMES=["application/pdf","image/jpeg","image/png"];const RECEIPT_ALLOWED_EXTENSIONS=["pdf","jpg","jpeg","png"];const RECEIPT_MAX_BYTES=10*1024*1024;const listContainer=document.querySelector(".guarantees-list");const scrollEnd=listContainer.querySelector("#scroll-end");const spinner=scrollEnd.querySelector(".spinner");initResizableColumns(table);const goConfig=window.__GO_CONFIG__||{};const restRoot=(goConfig.rest&&goConfig.rest.root)||(window.GO_REST&&window.GO_REST.root)||"/wp-json/";const restNonce=(goConfig.rest&&goConfig.rest.nonce)||(window.GO_REST&&window.GO_REST.nonce)||"";const userRole=(goConfig.user&&goConfig.user.role)||"user";const isAdmin=["administrator","admin","go_garantias","go_comercial","go_director_comercial",].includes(userRole);const isCoreAdmin=userRole==="administrator"||userRole==="admin";const isProfesional=userRole==="go_profesional"||userRole==="profesional";const ADMIN_SUMMARY_ERROR_MESSAGE="No hemos podido cargar los datos. Vuelve a intentarlo en unos segundos.";const ADMIN_SUMMARY_DEFAULT_CONTEXT="month";const ADMIN_SUMMARY_STATE_VALUES=["activada","pendiente_pago","pendiente_revision","sin_finalizar",];const ADMIN_SUMMARY_STATE_ORDER=[...ADMIN_SUMMARY_STATE_VALUES];const DONUT_BASE_COLOR_TOKEN="var(--donut-base-color)";const ADMIN_SUMMARY_STATE_COLORS={activada:{color:"var(--admin-summary-state-activada)",muted:"var(--admin-summary-state-activada-muted)",},pendiente_pago:{color:"var(--admin-summary-state-pendiente-pago)",muted:"var(--admin-summary-state-pendiente-pago-muted)",},pendiente_revision:{color:"var(--admin-summary-state-pendiente-revision)",muted:"var(--admin-summary-state-pendiente-revision-muted)",},sin_finalizar:{color:"var(--admin-summary-state-sin-finalizar)",muted:"var(--admin-summary-state-sin-finalizar-muted)",},};const ADMIN_SUMMARY_ACTION_ARROW_ICON='<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16"><path fill-rule="evenodd" d="M4.646 1.646a.5.5 0 0 1 .708 0l6 6a.5.5 0 0 1 0 .708l-6 6a.5.5 0 0 1-.708-.708L10.293 8 4.646 2.354a.5.5 0 0 1 0-.708z"/></svg>';const ADMIN_SUMMARY_ACTIONS=[{key:"payment",label:"Pendientes de pago",description:"Deben completarse los cobros pendientes de pago",filterValue:"pendiente_pago",icon:'<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16"><path d="M1.92.506a.5.5 0 0 1 .434.146L3 1.293l.646-.647a.5.5 0 0 1 .708 0L5 1.293l.646-.647a.5.5 0 0 1 .708 0L7 1.293l.646-.647a.5.5 0 0 1 .708 0L9 1.293l.646-.647a.5.5 0 0 1 .708 0l.646.647.646-.647a.5.5 0 0 1 .708 0l.646.647.646-.647a.5.5 0 0 1 .801.13l.5 1A.5.5 0 0 1 15 2v12a.5.5 0 0 1-.053.224l-.5 1a.5.5 0 0 1-.8.13L13 14.707l-.646.647a.5.5 0 0 1-.708 0L11 14.707l-.646.647a.5.5 0 0 1-.708 0L9 14.707l-.646.647a.5.5 0 0 1-.708 0L7 14.707l-.646.647a.5.5 0 0 1-.708 0L5 14.707l-.646.647a.5.5 0 0 1-.708 0L3 14.707l-.646.647a.5.5 0 0 1-.801-.13l-.5-1A.5.5 0 0 1 1 14V2a.5.5 0 0 1 .053-.224l.5-1a.5.5 0 0 1 .367-.27zm.217 1.338L2 2.118v11.764l.137.274.51-.51a.5.5 0 0 1 .707 0l.646.647.646-.646a.5.5 0 0 1 .708 0l.646.646.646-.646a.5.5 0 0 1 .708 0l.646.646.646-.646a.5.5 0 0 1 .708 0l.646.646.646-.646a.5.5 0 0 1 .708 0l.646.646.51.51.137-.274V2.118l-.137-.274-.51.51a.5.5 0 0 1-.707 0L12 1.707l-.646.647a.5.5 0 0 1-.708 0L10 1.707l-.646.647a.5.5 0 0 1-.708 0L8 1.707l-.646.647a.5.5 0 0 1-.708 0L6 1.707l-.646.647a.5.5 0 0 1-.708 0L4 1.707l-.646.647a.5.5 0 0 1-.708 0l-.51-.51z"/></svg>',accent:"var(--admin-summary-action-payment)",},{key:"validation",label:"Pendientes de verificar transferencia",description:"Verifica las transferencias recibidas",filterValue:"validacion_pendiente",icon:'<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16"><path d="M10.854 5.146a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708 0l-1.5-1.5a.5.5 0 1 1 .708-.708L7.5 7.793l2.646-2.647a.5.5 0 0 1 .708 0z"/><path d="M3.5 0a.5.5 0 0 1 .5.5V1h8V.5a.5.5 0 0 1 1 0V1h1a2 2 0 0 1 2 2v11a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h1V.5a.5.5 0 0 1 .5-.5zM1 4v10a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V4H1z"/></svg>',accent:"var(--admin-summary-action-validation)",},{key:"collect",label:"Pendientes de cobrar domiciliación",description:"Revisa las domiciliaciones en curso",filterValue:"pendiente_cobro",icon:'<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16"><path d="M10.854 5.146a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708 0l-1.5-1.5a.5.5 0 1 1 .708-.708L7.5 7.793l2.646-2.647a.5.5 0 0 1 .708 0z"/><path d="M3.5 0a.5.5 0 0 1 .5.5V1h8V.5a.5.5 0 0 1 1 0V1h1a2 2 0 0 1 2 2v11a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h1V.5a.5.5 0 0 1 .5-.5zM1 4v10a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V4H1z"/></svg>',accent:"var(--admin-summary-action-collect)",},];const integerFormatter=new Intl.NumberFormat("es-ES");const currencyFormatter=new Intl.NumberFormat("es-ES",{style:"currency",currency:"EUR",});let adminSummaryCache=null;let adminSummaryPromise=null;const numberAnimations=new WeakMap();const copyIcon='<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M360-240q-33 0-56.5-23.5T280-320v-480q0-33 23.5-56.5T360-880h360q33 0 56.5 23.5T800-800v480q0 33-23.5 56.5T720-240H360Zm0-80h360v-480H360v480ZM200-80q-33 0-56.5-23.5T120-160v-560h80v560h440v80H200Zm160-240v-480 480Z"/></svg>';const phoneIcon='<svg height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M798-120q-125 0-247-54.5T329-329Q229-429 174.5-551T120-798q0-18 12-30t30-12h162q14 0 25 9.5t13 22.5l26 140q2 16-1 27t-11 19l-97 98q20 37 47.5 71.5T387-386q31 31 65 57.5t72 48.5l94-94q9-9 23.5-13.5T670-390l138 28q14 4 23 14.5t9 23.5v162q0 18-12 30t-30 12ZM241-600l66-66-17-94h-89q5 41 14 81t26 79Zm358 358q39 17 79.5 27t81.5 13v-88l-94-19-67 67ZM241-600Zm358 358Z"/></svg>';const emailIcon='<svg height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M160-160q-33 0-56.5-23.5T80-240v-480q0-33 23.5-56.5T160-800h640q33 0 56.5 23.5T880-720v480q0 33-23.5 56.5T800-160H160Zm320-280L160-640v400h640v-400L480-440Zm0-80 320-200H160l320 200ZM160-640v-80 480-400Z"/></svg>';const userIcon='<svg height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M234-276q51-39 114-61.5T480-360q69 0 132 22.5T726-276q35-41 54.5-93T800-480q0-133-93.5-226.5T480-800q-133 0-226.5 93.5T160-480q0 59 19.5 111t54.5 93Zm246-164q-59 0-99.5-40.5T340-580q0-59 40.5-99.5T480-720q59 0 99.5 40.5T620-580q0 59-40.5 99.5T480-440Zm0 360q-83 0-156-31.5T197-197q-54-54-85.5-127T80-480q0-83 31.5-156T197-763q54-54 127-85.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 83-31.5 156T763-197q-54 54-127 85.5T480-80Zm0-80q53 0 100-15.5t86-44.5q-39-29-86-44.5T480-280q-53 0-100 15.5T294-220q39 29 86 44.5T480-160Zm0-360q26 0 43-17t17-43q0-26-17-43t-43-17q-26 0-43 17t-17 43q0 26 17 43t43 17Zm0-60Zm0 360Z"/></svg>';const warningIcon='<svg height="24" viewBox="0 -960 960 960" width="24" fill="currentColor"><path d="m40-120 440-760 440 760H40Zm138-80h604L480-720 178-200Zm302-40q17 0 28.5-11.5T520-280q0-17-11.5-28.5T480-320q-17 0-28.5 11.5T440-280q0 17 11.5 28.5T480-240Zm-40-120h80v-200h-80v200Zm40-100Z"/></svg>';const heartIcon='<svg height="24" viewBox="0 -960 960 960" width="24" fill="currentColor"><path d="m480-120-58-52q-101-91-167-157T150-447.5Q111-500 95.5-544T80-634q0-94 63-157t157-63q52 0 99 22t81 62q34-40 81-62t99-22q94 0 157 63t63 157q0 46-15.5 90T810-447.5Q771-395 705-329T538-172l-58 52Zm0-108q96-86 158-147.5t98-107q36-45.5 50-81t14-70.5q0-60-40-100t-100-40q-47 0-87 26.5T518-680h-76q-15-41-55-67.5T300-774q-60 0-100 40t-40 100q0 35 14 70.5t50 81q36 45.5 98 107T480-228Zm0-273Z"/></svg>';const shareIcon='<svg height="24" viewBox="0 -960 960 960" width="24" fill="currentColor"><path d="M680-80q-50 0-85-35t-35-85q0-6 3-28L282-392q-16 15-37 23.5t-45 8.5q-50 0-85-35t-35-85q0-50 35-85t85-35q24 0 45 8.5t37 23.5l281-164q-2-7-2.5-13.5T560-760q0-50 35-85t85-35q50 0 85 35t35 85q0 50-35 85t-85 35q-24 0-45-8.5T598-672L317-508q2 7 2.5 13.5t.5 14.5q0 8-.5 14.5T317-452l281 164q16-15 37-23.5t45-8.5q50 0 85 35t35 85q0 50-35 85t-85 35Zm0-80q17 0 28.5-11.5T720-200q0-17-11.5-28.5T680-240q-17 0-28.5 11.5T640-200q0 17 11.5 28.5T680-160ZM200-440q17 0 28.5-11.5T240-480q0-17-11.5-28.5T200-520q-17 0-28.5 11.5T160-480q0 17 11.5 28.5T200-440Zm480-280q17 0 28.5-11.5T720-760q0-17-11.5-28.5T680-800q-17 0-28.5 11.5T640-760q0 17 11.5 28.5T680-720Zm0 520ZM200-480Zm480-280Z"/></svg>';const personAddIcon='<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M720-400v-120H600v-80h120v-120h80v120h120v80H800v120h-80Zm-360-80q-66 0-113-47t-47-113q0-66 47-113t113-47q66 0 113 47t47 113q0 66-47 113t-113 47ZM40-160v-112q0-34 17.5-62.5T104-378q62-31 126-46.5T360-440q66 0 130 15.5T616-378q29 15 46.5 43.5T680-272v112H40Zm80-80h480v-32q0-11-5.5-20T580-306q-54-27-109-40.5T360-360q-56 0-111 13.5T140-306q-9 5-14.5 14t-5.5 20v32Zm240-320q33 0 56.5-23.5T440-640q0-33-23.5-56.5T360-720q-33 0-56.5 23.5T280-640q0 33 23.5 56.5T360-560Zm0-80Zm0 400Z"/></svg>';const personIcon='<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M480-480q-66 0-113-47t-47-113q0-66 47-113t113-47q66 0 113 47t47 113q0 66-47 113t-113 47ZM160-160v-112q0-34 17.5-62.5T224-378q62-31 126-46.5T480-440q66 0 130 15.5T736-378q29 15 46.5 43.5T800-272v112H160Zm80-80h480v-32q0-11-5.5-20T700-306q-54-27-109-40.5T480-360q-56 0-111 13.5T260-306q-9 5-14.5 14t-5.5 20v32Zm240-320q33 0 56.5-23.5T560-640q0-33-23.5-56.5T480-720q-33 0-56.5 23.5T400-640q0 33 23.5 56.5T480-560Zm0-80Zm0 400Z"/></svg>';const paymentIcon='<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M560-440q-50 0-85-35t-35-85q0-50 35-85t85-35q50 0 85 35t35 85q0 50-35 85t-85 35ZM280-320q-33 0-56.5-23.5T200-400v-320q0-33 23.5-56.5T280-800h560q33 0 56.5 23.5T920-720v320q0 33-23.5 56.5T840-320H280Zm80-80h400q0-33 23.5-56.5T840-480v-160q-33 0-56.5-23.5T760-720H360q0 33-23.5 56.5T280-640v160q33 0 56.5 23.5T360-400Zm440 240H120q-33 0-56.5-23.5T40-240v-440h80v440h680v80ZM280-400v-320 320Z"/></svg>';const continueIcon='<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M200-200h57l391-391-57-57-391 391v57Zm-80 80v-170l528-527q12-11 26.5-17t30.5-6q16 0 31 6t26 18l55 56q12 11 17.5 26t5.5 30q0 16-5.5 30.5T817-647L290-120H120Zm640-584-56-56 56 56Zm-141 85-28-29 57 57-29-28Z"/></svg>';const pdfIcon=(goConfig.icons&&goConfig.icons.pdf)||"";const downloadIcon=(goConfig.icons&&goConfig.icons.download)||"";const plusIcon=(goConfig.icons&&goConfig.icons.plus)||"";const arrowDownIcon=(goConfig.icons&&goConfig.icons.arrowDropDown)||"";const arrowUpIcon=(goConfig.icons&&goConfig.icons.arrowDropUp)||"";const arrowLeftIcon=(goConfig.icons&&goConfig.icons.arrowLeft)||'<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="currentColor"><path d="M400-240 160-480l240-240 56 58-142 142h486v80H314l142 142-56 58Z"/></svg>';let pendingConfirmContext=null;const confirmModalController=setupConfirmModal(document.querySelector(".confirm-modal"));const PDF_CACHE_LIMIT=12;const pdfBlobCache=new Map();const pdfBlobOrder=[];const pdfBlobPromises=new Map();function normalizeDocUrl(url){return typeof url==="string"?url.trim():"";}
function touchPdfCache(key){const index=pdfBlobOrder.indexOf(key);if(index!==-1){pdfBlobOrder.splice(index,1);}
pdfBlobOrder.push(key);}
function evictPdfCache(){while(pdfBlobOrder.length>PDF_CACHE_LIMIT){const oldest=pdfBlobOrder.shift();if(!oldest){break;}
const cached=pdfBlobCache.get(oldest);if(cached&&cached.objectUrl){URL.revokeObjectURL(cached.objectUrl);}
pdfBlobCache.delete(oldest);}}
function storePdfBlob(key,blob){const objectUrl=URL.createObjectURL(blob);pdfBlobCache.set(key,{objectUrl,timestamp:Date.now()});touchPdfCache(key);evictPdfCache();return objectUrl;}
function getCachedDocumentObjectUrl(url){const normalized=normalizeDocUrl(url);if(!normalized){return"";}
const cached=pdfBlobCache.get(normalized);if(!cached){return"";}
touchPdfCache(normalized);return cached.objectUrl;}
function ensureDocumentPreloaded(url){const normalized=normalizeDocUrl(url);if(!normalized){return Promise.resolve("");}
if(pdfBlobCache.has(normalized)){touchPdfCache(normalized);return Promise.resolve(pdfBlobCache.get(normalized).objectUrl);}
if(pdfBlobPromises.has(normalized)){return pdfBlobPromises.get(normalized);}
const fetchPromise=fetch(normalized,{credentials:"same-origin",}).then((res)=>{if(!res.ok){throw new Error(`Error HTTP ${res.status}`);}
return res.blob();}).then((blob)=>{if(!blob||blob.size===0){throw new Error("Documento vacío");}
const type=(blob.type||"").toLowerCase();if(type){const isKnownMime=RECEIPT_ALLOWED_MIMES.includes(type)||type.includes("pdf");if(!isKnownMime){console.warn("Contenido no es PDF, se intentará mostrar igualmente",type);}}
pdfBlobPromises.delete(normalized);return storePdfBlob(normalized,blob);}).catch((error)=>{pdfBlobPromises.delete(normalized);throw error;});pdfBlobPromises.set(normalized,fetchPromise);return fetchPromise;}
function scheduleDocumentPreload(url){const normalized=normalizeDocUrl(url);if(!normalized){return;}
ensureDocumentPreloaded(normalized).catch((error)=>{console.warn("No se pudo precargar el documento",error);});}
const misGarantiasBase=(goConfig.pages&&goConfig.pages.misGarantias)||"/garantias-online/mis-garantias/";const newGuaranteeUrl=(goConfig.pages&&goConfig.pages.nuevaGarantia)||"/garantias-online/nueva-garantia/";const SHARE_UNAVAILABLE_MESSAGE="La función de compartir no está disponible en este navegador.";const saveStatus=document.createElement("div");saveStatus.className="autosave-status autosave-status--hidden";saveStatus.innerHTML='<span class="autosave-status__spinner"></span>'+'<span class="autosave-status__icon" style="display:none"></span>'+'<span class="autosave-status__text">Guardando</span>';document.body.appendChild(saveStatus);const saveStatusText=saveStatus.querySelector(".autosave-status__text");const DEFAULT_PER=12;let perPage=DEFAULT_PER;let currentPage=1;let totalPages=1;let totalPosts=0;let isLoading=false;let hasMore=true;let searchQuery="";let lastValidQuery="";let lastValidResults=[];const detail=document.querySelector(".guarantee-detail");let panel1=document.getElementById("detail-panel-1");let panel2=document.getElementById("detail-panel-2");let lastEmptyPanel=panel1;const emptyTemplatesWrapper=document.querySelector(".guarantee-detail__empty-templates");const emptyTemplateMap={};if(emptyTemplatesWrapper){emptyTemplatesWrapper.querySelectorAll("[data-empty-template]").forEach((node)=>{const key=node.getAttribute("data-empty-template");if(key){emptyTemplateMap[key]=node.innerHTML.trim();}});}
if(isCoreAdmin&&panel1&&panel1.classList.contains("active")){initializeAdminSummary(panel1);}
let currentEmptyMode="awaiting";const filtersRoot=document.querySelector(".guarantees-list__filters");const estadoSelect=document.querySelector('[data-filter="estado"]');const planSelect=document.querySelector('[data-filter="plan"]');const canalSelect=document.querySelector('[data-filter="canal"]');const concesionarioSelect=document.querySelector('[data-filter="cliente"]');const paymentSelect=document.querySelector('[data-filter="payment"]');const commercialSelect=document.querySelector('[data-filter="commercial"]');const clientsWrapper=document.querySelector("[data-clients-wrapper]");const moreFiltersToggle=document.querySelector("[data-more-filters]");const advancedPanel=document.querySelector("[data-advanced-panel]");const resetFiltersBtn=document.querySelector("[data-reset-filters]");const orderRoot=document.querySelector("[data-order-root]");const orderToggle=orderRoot?orderRoot.querySelector("[data-order-toggle]"):null;const orderMenu=orderRoot?orderRoot.querySelector("[data-order-menu]"):null;const orderLabelNode=orderRoot?orderRoot.querySelector("[data-order-label]"):null;const rootElement=document.documentElement;let selectedEstado="";let selectedPlan="";let selectedCanal="";let selectedConcesionario="";let selectedVendorType="";let selectedPaymentMethod="";let selectedCommercial="";const defaultOrderKey=orderToggle?orderToggle.getAttribute("data-default-sort")||"created_desc":"created_desc";let selectedOrderKey=defaultOrderKey;let selectedOrderBy="created";let selectedOrderDirection=defaultOrderKey.endsWith("_asc")?"asc":"desc";let currentListAbort=null;let rawClients=[];let rawCommercials=[];const orderOptions=new Map();let setAdvancedOpen=()=>{};let baseFiltersHeight=filtersRoot?filtersRoot.offsetHeight||0:0;const updateBaseFiltersHeight=()=>{if(!filtersRoot){baseFiltersHeight=0;return;}
baseFiltersHeight=filtersRoot.offsetHeight||0;};const updateAdvancedHeight=()=>{if(!filtersRoot){rootElement.style.setProperty("--go-advanced-filters-height","0px");baseFiltersHeight=0;return;}
const isAdvancedVisible=advancedPanel&&!advancedPanel.hasAttribute("hidden");if(!isAdvancedVisible){updateBaseFiltersHeight();rootElement.style.setProperty("--go-advanced-filters-height","0px");return;}
const currentHeight=filtersRoot.offsetHeight||0;const baseline=baseFiltersHeight||0;const extra=Math.max(0,Math.round(currentHeight-baseline));rootElement.style.setProperty("--go-advanced-filters-height",`${extra}px`);};updateBaseFiltersHeight();const hasActiveFilters=()=>{const hasSearch=Boolean((searchQuery||"").trim());if(hasSearch){return true;}
if(selectedEstado||selectedPlan||selectedCanal||selectedConcesionario||selectedVendorType||selectedPaymentMethod||selectedCommercial){return true;}
if(orderToggle&&selectedOrderKey!==defaultOrderKey){return true;}
return false;};const updateResetVisibility=()=>{if(!resetFiltersBtn){return;}
resetFiltersBtn.hidden=!hasActiveFilters();};updateResetVisibility();if(typeof ResizeObserver!=="undefined"&&advancedPanel){const resizeObserver=new ResizeObserver(()=>{if(!advancedPanel.hasAttribute("hidden")){updateAdvancedHeight();}});resizeObserver.observe(advancedPanel);}
window.addEventListener("resize",()=>{if(advancedPanel&&!advancedPanel.hasAttribute("hidden")){updateAdvancedHeight();}else{updateBaseFiltersHeight();}});let resultMessage=document.querySelector(".guarantees-list__result-message");if(!resultMessage){resultMessage=document.createElement("div");resultMessage.className="guarantees-list__result-message";resultMessage.style.display="none";table.insertAdjacentElement("afterend",resultMessage);}
if(!panel1||!panel2){detail.innerHTML='<div class="guarantee-detail__panel active" id="detail-panel-1">'+
detail.innerHTML+'</div><div class="guarantee-detail__panel" id="detail-panel-2"></div>';panel1=document.getElementById("detail-panel-1");panel2=document.getElementById("detail-panel-2");}
let activePanel=panel1;let inactivePanel=panel2;activePanel.classList.add("active");inactivePanel.classList.remove("active");let prevSelectedRow=null;let prevIdx=null;const urlMat=new URLSearchParams(window.location.search).get("matricula");let pendingMatSelection=Boolean(urlMat);let initialMatQuery=typeof urlMat==="string"?urlMat.trim():"";const detailCache=new Map();const detailPromises=new Map();const loadedIds=new Set();const listCache=new Map();function buildListCacheKey(search="",estado="",plan="",canal="",concesionario="",vendorType="",payment="",orderBy="",orderDirection="",commercial=""){return[search,estado,plan,canal,concesionario,vendorType,payment,orderBy,orderDirection,commercial,].join("|");}
function renderFromCache(cache){for(const item of cache.data){tbody.appendChild(renderRow(item));loadedIds.add(item.id);}
totalPages=cache.totalPages;totalPosts=cache.totalPosts;hasMore=currentPage<totalPages;spinner.style.display=hasMore?"":"none";if(!hasActiveFilters()&&(cache.totalPosts||0)===0){setEmptyDetailPanel("forward","no-results");}}
function getSelectedChannelData(){if(!canalSelect){return{channel:"",vendorType:""};}
const option=canalSelect.options[canalSelect.selectedIndex];if(!option){return{channel:"",vendorType:""};}
const dataset=option.dataset||{};const channel=dataset.channel||option.value||"";const vendorType=dataset.vendorType||"";return{channel,vendorType};}
function populateClientsSelect(filterType=""){if(!concesionarioSelect){return;}
const previous=concesionarioSelect.value;concesionarioSelect.querySelectorAll("option:not(:first-child)").forEach((opt)=>opt.remove());if(!Array.isArray(rawClients)||rawClients.length===0){selectedConcesionario="";return;}
const filtered=rawClients.filter((client)=>{if(!filterType){return true;}
return(client.type||"")===filterType;});filtered.forEach((client)=>{const opt=document.createElement("option");opt.value=String(client.id);opt.textContent=client.name;if(client.type){opt.dataset.type=client.type;}
concesionarioSelect.appendChild(opt);});if(previous&&filtered.some((client)=>String(client.id)===previous)){concesionarioSelect.value=previous;selectedConcesionario=previous;}else{concesionarioSelect.value="";selectedConcesionario="";}
if(!advancedPanel||advancedPanel.hasAttribute("hidden")){updateBaseFiltersHeight();}
updateResetVisibility();}
function refreshClientsVisibility(){const{channel,vendorType}=getSelectedChannelData();selectedCanal=channel;if(!clientsWrapper){selectedVendorType="";return;}
selectedVendorType=vendorType;const shouldShow=channel==="profesional"||channel==="gestoria";if(shouldShow){clientsWrapper.hidden=false;}else{clientsWrapper.hidden=true;}
if(!shouldShow){selectedConcesionario="";if(concesionarioSelect){concesionarioSelect.value="";}}
populateClientsSelect(shouldShow?vendorType:"");if(!advancedPanel||advancedPanel.hasAttribute("hidden")){updateBaseFiltersHeight();}
updateResetVisibility();}
function populateCommercialSelect(){if(!commercialSelect){return;}
const previous=commercialSelect.value||"";commercialSelect.querySelectorAll("option:not(:first-child)").forEach((opt)=>opt.remove());if(!Array.isArray(rawCommercials)||rawCommercials.length===0){selectedCommercial="";commercialSelect.value="";return;}
let hasPrevious=false;rawCommercials.forEach((item)=>{const id=item&&typeof item.id!=="undefined"?item.id:null;const name=item&&typeof item.name==="string"?item.name:"";const numericId=Number.parseInt(id,10);if(!Number.isFinite(numericId)||numericId<=0||!name){return;}
const option=document.createElement("option");option.value=String(numericId);option.textContent=name;commercialSelect.appendChild(option);if(!hasPrevious&&previous&&String(numericId)===previous){hasPrevious=true;}});if(previous&&hasPrevious){commercialSelect.value=previous;selectedCommercial=previous;}else if(previous&&!hasPrevious){commercialSelect.value="";if(selectedCommercial){selectedCommercial="";applyFilters();}}else{selectedCommercial=commercialSelect.value||"";}
updateResetVisibility();}
function fetchDetail(id){if(detailCache.has(id)){return Promise.resolve(detailCache.get(id));}
if(detailPromises.has(id)){return detailPromises.get(id);}
const p=fetch(`${restRoot}go/v1/guarantees/${id}`,{headers:{"X-WP-Nonce":restNonce},}).then((res)=>{if(!res.ok)throw res.status;return res.json();}).then((json)=>{const data=normalizeDetailData(json);detailCache.set(id,data);detailPromises.delete(id);return data;}).catch((err)=>{detailPromises.delete(id);throw err;});detailPromises.set(id,p);return p;}
document.addEventListener("click",(e)=>{const btn=e.target.closest(".guarantee-detail__btn--continue");if(!btn)return;const panel=btn.closest(".guarantee-detail__panel");const id=panel?.dataset.loadedId;if(id){localStorage.setItem("go_draft_id",id);const data=detailCache.get(id);if(data&&data.uuid){localStorage.setItem("go_draft_uuid",data.uuid);}}
const data=id?detailCache.get(id):null;const uuid=data&&data.uuid?data.uuid:"";window.location.href="/garantias-online/nueva-garantia?uuid="+
encodeURIComponent(uuid);});function formatAmountForMessage(raw){if(raw===undefined||raw===null)return"";const formatted=formatPrice(raw);if(formatted&&formatted!=="-"&&formatted.trim()!==""){return formatted.includes("€")?formatted:`${formatted} €`;}
const str=String(raw).trim();if(!str)return"";return str.includes("€")?str:`${str} €`;}
function escapeHtml(value){if(value===undefined||value===null){return"";}
return String(value).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;");}
function parseDisplayDate(value){if(typeof value!=="string"){return null;}
const trimmed=value.trim();if(!trimmed){return null;}
const parts=trimmed.split(/[\/]/);if(parts.length<3){return null;}
const day=parseInt(parts[0],10);const month=parseInt(parts[1],10);let year=parseInt(parts[2],10);if(Number.isNaN(day)||Number.isNaN(month)||Number.isNaN(year)){return null;}
if(year<100){year+=2000;}
const date=new Date(year,month-1,day);if(Number.isNaN(date.getTime())){return null;}
return date;}
function handleConfirmClick(e){const btn=e.target.closest(".guarantee-detail__btn--confirm");if(!btn)return;const panel=btn.closest(".guarantee-detail__panel");if(!panel)return;const id=panel.dataset.loadedId;if(!id)return;const cacheData=detailCache.get(id);const uuid=cacheData&&cacheData.uuid?cacheData.uuid:"";if(!uuid)return;const textSpan=btn.querySelector(".guarantee-detail__btn-text");const currentLabel=textSpan?textSpan.textContent.trim():"";const row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);const metodoRaw=(cacheData&&cacheData.metodo_pago)||(row&&row.dataset.metodoPago)||"";const metodo=typeof metodoRaw==="string"?metodoRaw.toLowerCase().trim():"";const isDomiciliacion=metodo.startsWith("domiciliacion");const vendorName=(cacheData&&cacheData.concesionario)||(row&&row.dataset.vendedor_name)||"";const priceRaw=(cacheData&&cacheData.precio)||(row&&row.dataset.precio)||"";const formattedAmount=formatAmountForMessage(priceRaw);const accountCandidate=(cacheData&&(cacheData.transfer_iban||cacheData.iban_vendedor))||(row&&(row.dataset.transferIban||row.dataset.ibanVendedor))||"";const accountTextRaw=typeof accountCandidate==="string"?accountCandidate.trim():"";const matricula=panel.dataset.matricula||(row&&row.dataset.matricula)||(cacheData&&cacheData.matricula)||"";const title=isDomiciliacion?"Confirmar cobro por domiciliación":"Confirmar transferencia";const safeSubtitleId=escapeHtml(matricula||id);const subtitle=safeSubtitleId?`Garantía <strong>${safeSubtitleId}</strong>`:"";const cleanedVendorName=typeof vendorName==="string"?vendorName.trim():"";const companyText=cleanedVendorName!==""&&cleanedVendorName!=="-"?cleanedVendorName:"el cliente";const normalizedAmount=formattedAmount.trim();const hasAmount=normalizedAmount!==""&&normalizedAmount!=="-"&&normalizedAmount!=="- €";const amountText=hasAmount?normalizedAmount:"el importe acordado";const hasAccount=accountTextRaw!==""&&accountTextRaw!=="-";const accountText=hasAccount?accountTextRaw:"";const periodStartDisplay=(cacheData&&cacheData.desde_fmt)||(row&&row.dataset.desdeFmt)||"";const safeCompany=escapeHtml(companyText);const safeAmount=escapeHtml(amountText);const safeAccount=escapeHtml(accountText);let message;if(isDomiciliacion){message=`Confirmo que 360VO ha gestionado el cobro por domiciliación por valor de <strong>${safeAmount}</strong> a <strong>${safeCompany}</strong>.`;}else if(safeAccount){message=`Confirmo que <strong>${safeCompany}</strong> ha realizado la transferencia por valor de <strong>${safeAmount}</strong> a la cuenta ${safeAccount}.`;}else{message=`Confirmo que <strong>${safeCompany}</strong> ha realizado la transferencia por valor de <strong>${safeAmount}</strong>.`;}
const startIso=(row&&row.dataset.desde)||(cacheData&&cacheData.desde)||"";let paymentWindowText="";if(!isDomiciliacion){let startDate=null;if(startIso){const parsedIso=new Date(startIso);if(!Number.isNaN(parsedIso.getTime())){startDate=parsedIso;}}
if(!startDate&&periodStartDisplay){startDate=parseDisplayDate(periodStartDisplay);}
if(startDate){const formatter=new Intl.DateTimeFormat("es-ES",{day:"2-digit",month:"2-digit",year:"2-digit",});const deadline=new Date(startDate.getTime()+
48*60*60*1000);const startDisplay=formatter.format(startDate);const deadlineDisplay=formatter.format(deadline);paymentWindowText=`Plazo de pago: del ${startDisplay} al ${deadlineDisplay}`;}}
if(paymentWindowText){const escapedWindow=escapeHtml(paymentWindowText);message+=` <span class="confirm-modal__payment-window">${escapedWindow}</span>`;}
const note=!isDomiciliacion?"La garantía se activará.":"";const context={intent:"activate",btn,panel,id,uuid,metodo,cacheData,row,resetLabel:currentLabel||(isDomiciliacion?"Confirmar domiciliación":"Confirmar pago"),};if(confirmModalController){pendingConfirmContext=context;confirmModalController.open({title,subtitle,message,note,confirmLabel:"Confirmar",requireAcknowledgement:true,});return;}
runConfirmRequest(context);}
function handleTransferReportClick(event){const btn=event.target.closest(".guarantee-detail__btn--transfer-report");if(!btn){return;}
const panel=btn.closest(".guarantee-detail__panel");if(!panel){return;}
const id=panel.dataset.loadedId;if(!id){return;}
const cacheData=detailCache.get(id);if(!cacheData){return;}
const row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);const matricula=cacheData.matricula||(row&&row.dataset.matricula)||panel.dataset.matricula||"";const conceptRaw=`Garantía ${matricula || id}`;const amountRaw=formatAmountForMessage(cacheData.precio||(row&&row.dataset.precio)||"");const normalizedAmount=amountRaw.trim();const hasAmount=normalizedAmount!==""&&normalizedAmount!=="-"&&normalizedAmount!=="- €";const transferAccount=(()=>{const candidate=cacheData.transfer_iban||cacheData.iban_vendedor||(row&&(row.dataset.transferIban||row.dataset.ibanVendedor))||"";return typeof candidate==="string"?candidate.trim():"";})();const safeConcept=escapeHtml(conceptRaw);const safeAmount=hasAmount?escapeHtml(normalizedAmount):"";const hasAccount=transferAccount!==""&&transferAccount!=="-";const safeAccount=hasAccount?escapeHtml(transferAccount):"";const safeSubtitleId=escapeHtml(matricula||id);let message="Adjunta el comprobante de la transferencia";if(safeAmount){message+=` por <strong>${safeAmount}</strong>`;}
if(safeAccount){message+=` realizada a la cuenta <strong>${safeAccount}</strong>`;}
message+=`, utilizando el concepto <strong>${safeConcept}</strong>.`;const note="Validaremos la operación y recibirás un correo cuando la garantía esté activa.";const subtitle=safeSubtitleId?`Garantía <strong>${safeSubtitleId}</strong>`:"";const context={intent:"transfer-report",btn,panel,id,row,confirmPayload:{concept:conceptRaw,amount:hasAmount?normalizedAmount:"",account:hasAccount?transferAccount:"",},resetLabel:btn.querySelector(".guarantee-detail__btn-text")?.textContent.trim()||"Ya he realizado la transferencia",};if(confirmModalController){pendingConfirmContext=context;confirmModalController.open({title:"Confirmación de Pago",subtitle,message,note,confirmLabel:"Enviar comprobante",requireFile:true,});return;}
runConfirmRequest(context);}
function runConfirmRequest(context){if(!context)return;if(context.intent==="transfer-report"){runTransferReportRequest(context);return;}
const{btn,panel,id,uuid}=context;if(!btn||!panel||!id||!uuid)return;const textSpan=btn.querySelector(".guarantee-detail__btn-text");const metodo=typeof context.metodo==="string"?context.metodo.toLowerCase():"";const resetText=context.resetLabel||(textSpan?textSpan.textContent.trim():metodo.startsWith("domiciliacion")?"Confirmar domiciliación":"Confirmar pago");let spinner=btn.querySelector(".guarantee-detail__btn-spinner");if(!spinner){spinner=document.createElement("span");spinner.className="guarantee-detail__btn-spinner";if(textSpan){btn.insertBefore(spinner,textSpan);}else{btn.appendChild(spinner);}}
if(textSpan){textSpan.textContent="Activando garantía";}
btn.disabled=true;saveStatus.classList.remove("autosave-status--hidden");let row=context.row;if(!row||!row.isConnected){row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);}
const body={id,uuid,data:{estado_garantia:{estado_contratacion:"activada",},},};if(metodo.startsWith("domiciliacion")){body.data.garantia_contratada={estado_cobro:{cobro_realizado:true},};}
fetch(`${restRoot}go/v1/guarantees/autosave`,{method:"POST",headers:{"Content-Type":"application/json","X-WP-Nonce":restNonce,},body:JSON.stringify(body),}).then((res)=>{if(!res.ok)throw res.status;detailCache.delete(id);if(!row||!row.isConnected){row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);}
if(row){row.dataset.estadoclase="activada";row.dataset.estado="Activada";row.dataset.cobroRealizado="1";const badge=row.querySelector(".guarantees-list__badge");if(badge){badge.textContent="Activada";badge.className="guarantees-list__badge guarantees-list__badge--activada";}
const cobroBadge=row.querySelector(".guarantees-list__badge--pend-cobro");if(cobroBadge){cobroBadge.remove();}}
return fetch(`${restRoot}go/v1/guarantees/${id}`,{headers:{"X-WP-Nonce":restNonce},});}).then((detailRes)=>{if(!detailRes.ok)throw detailRes.status;const rowData=row?buildRowData(row):{};return detailRes.json().then((json)=>{const data=normalizeDetailData(json);detailCache.set(id,data);panel.innerHTML=renderFullDetail(data,rowData,[]);setupTransferCountdown(panel);panel.dataset.matricula=data.matricula||rowData.matricula||"";panel.dataset.plan=data.plan||rowData.plan||"";syncPdfModalDocs(panel);});}).catch((err)=>{console.error("Error confirm payment:",err);}).finally(()=>{btn.disabled=false;saveStatus.classList.add("autosave-status--hidden");if(textSpan){const fallbackLabel=metodo.startsWith("domiciliacion")?"Confirmar domiciliación":"Confirmar pago";textSpan.textContent=resetText||fallbackLabel;}
if(spinner&&spinner.parentNode){spinner.remove();}});}
function runTransferReportRequest(context){const{btn,panel,id}=context;if(!btn||!panel||!id)return;const textSpan=btn.querySelector(".guarantee-detail__btn-text");const resetText=context.resetLabel||(textSpan?textSpan.textContent.trim():"Ya he realizado la transferencia");let spinner=btn.querySelector(".guarantee-detail__btn-spinner");if(!spinner){spinner=document.createElement("span");spinner.className="guarantee-detail__btn-spinner";if(textSpan){btn.insertBefore(spinner,textSpan);}else{btn.appendChild(spinner);}}
if(textSpan){textSpan.textContent="Enviando aviso";}
btn.disabled=true;if(saveStatusText){saveStatusText.textContent="Enviando aviso";}
saveStatus.classList.remove("autosave-status--hidden");const payload=context.confirmPayload||{};const formData=new FormData();formData.append("concept",payload.concept||"");formData.append("amount",payload.amount||"");formData.append("account",payload.account||"");if(context.receiptFile instanceof File){formData.append("receipt",context.receiptFile,context.receiptFile.name||"justificante");}
context.receiptFile=null;let row=context.row;if(!row||!row.isConnected){row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);}
fetch(`${restRoot}go/v1/guarantees/${id}/confirm-transfer`,{method:"POST",headers:{"X-WP-Nonce":restNonce,},body:formData,}).then(async(res)=>{if(!res.ok){let message="No se pudo enviar la confirmación. Inténtalo de nuevo.";try{const data=await res.json();if(data&&typeof data.message==="string"&&data.message.trim()!==""){message=data.message;}}catch(jsonError){try{const text=await res.text();if(typeof text==="string"&&text.trim()!==""){message=text;}}catch(textError){}}
throw new Error(message);}
return res.json();}).then((json)=>{const detailResponse=json?.detail||json;if(!detailResponse){return;}
const data=normalizeDetailData(detailResponse);detailCache.set(id,data);if(!row||!row.isConnected){row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);}
const newEstadoValue=(data.estado&&data.estado.value)||"validacion_pendiente";const newEstadoLabel=(data.estado&&data.estado.label)||"Validación pendiente";const newEstadoClase=normalizeEstadoClase(newEstadoValue);if(row){row.dataset.estadoclase=newEstadoClase;row.dataset.estado=newEstadoLabel;const badge=row.querySelector(".guarantees-list__badge");if(badge){badge.className="guarantees-list__badge guarantees-list__badge--"+
newEstadoClase;badge.textContent=newEstadoLabel;}}
const rowData=row?buildRowData(row):{};panel.innerHTML=renderFullDetail(data,rowData,[]);panel.dataset.matricula=data.matricula||rowData.matricula||"";panel.dataset.plan=data.plan||rowData.plan||"";panel.dataset.loadedId=id;setupTransferCountdown(panel);syncPdfModalDocs(panel);showDetailToast(panel,"Hemos avisado a 360VO. Revisarán la transferencia en las próximas horas.");}).catch((err)=>{console.error("Error reporting transfer:",err);const message=err instanceof Error&&err.message?err.message:"No se pudo enviar la confirmación. Inténtalo de nuevo.";showDetailToast(panel,message);}).finally(()=>{btn.disabled=false;if(textSpan){textSpan.textContent=resetText;}
if(spinner&&spinner.parentNode){spinner.remove();}
if(saveStatusText){saveStatusText.textContent="Guardando";}
saveStatus.classList.add("autosave-status--hidden");});}
document.addEventListener("click",handleConfirmClick);document.addEventListener("click",handleTransferReportClick);document.addEventListener("click",handleShareClick);function setupConfirmModal(modal){if(!modal)return null;const dialog=modal.querySelector(".confirm-modal__dialog");if(!dialog)return null;const titleEl=modal.querySelector(".confirm-modal__title");const subtitleEl=modal.querySelector(".confirm-modal__subtitle");const messageEl=modal.querySelector(".confirm-modal__message");const noteEl=modal.querySelector(".confirm-modal__note");const confirmBtn=modal.querySelector(".confirm-modal__btn--confirm");const cancelBtn=modal.querySelector(".confirm-modal__btn--cancel");const closeBtn=modal.querySelector(".confirm-modal__close");const uploadBlock=modal.querySelector(".confirm-modal__upload");const fileInput=modal.querySelector(".confirm-modal__file-input");const fileNameEl=modal.querySelector(".confirm-modal__file-name");const fileErrorEl=modal.querySelector(".confirm-modal__file-error");const checkboxWrapper=modal.querySelector(".confirm-modal__checkbox");const checkboxInput=modal.querySelector(".confirm-modal__checkbox-input");const checkboxLabel=modal.querySelector(".confirm-modal__checkbox-label");const fileEmptyText=fileNameEl?fileNameEl.dataset.empty||"":"";let requiresFile=false;let requiresAcknowledgement=false;let selectedFile=null;let fileErrorMessage="";function setFileError(message){fileErrorMessage=message;if(fileErrorEl){fileErrorEl.textContent=message;fileErrorEl.hidden=message==="";}
updateConfirmState();}
function resetFileState(){selectedFile=null;if(fileInput){fileInput.value="";}
if(fileNameEl){fileNameEl.textContent=fileEmptyText;}
setFileError("");}
function updateConfirmState(){if(!confirmBtn)return;const fileOk=!requiresFile||(selectedFile instanceof File&&fileErrorMessage==="");const ackOk=!requiresAcknowledgement||(checkboxInput?checkboxInput.checked:false);confirmBtn.disabled=!(fileOk&&ackOk);}
function closeModal(){modal.classList.remove("is-open");modal.setAttribute("aria-hidden","true");if(subtitleEl){subtitleEl.innerHTML="";subtitleEl.hidden=true;}
if(messageEl){messageEl.innerHTML="";}
if(noteEl){noteEl.textContent="";noteEl.hidden=true;}
if(uploadBlock){uploadBlock.hidden=true;}
requiresFile=false;requiresAcknowledgement=false;resetFileState();if(checkboxInput){checkboxInput.checked=false;}
if(checkboxWrapper){checkboxWrapper.hidden=true;}
if(confirmBtn){confirmBtn.disabled=true;}
document.removeEventListener("keydown",onKeydown);pendingConfirmContext=null;}
function openModal(content){if(!confirmBtn||!titleEl||!messageEl)return;const cfg=content||{};titleEl.textContent=cfg.title||"";if(subtitleEl){const hasSubtitle=Boolean(cfg.subtitle);subtitleEl.innerHTML=hasSubtitle?cfg.subtitle:"";subtitleEl.hidden=!hasSubtitle;}
messageEl.innerHTML=cfg.message||"";if(noteEl){const hasNote=Boolean(cfg.note);noteEl.textContent=cfg.note||"";noteEl.hidden=!hasNote;}
requiresFile=Boolean(cfg.requireFile);requiresAcknowledgement=Boolean(cfg.requireAcknowledgement);if(uploadBlock){uploadBlock.hidden=!requiresFile;}
if(checkboxWrapper){checkboxWrapper.hidden=!requiresAcknowledgement;}
if(checkboxLabel&&cfg.checkboxLabel){checkboxLabel.textContent=cfg.checkboxLabel;}
if(checkboxInput){checkboxInput.checked=false;}
resetFileState();confirmBtn.textContent=cfg.confirmLabel||"Confirmar";updateConfirmState();modal.classList.add("is-open");modal.setAttribute("aria-hidden","false");document.addEventListener("keydown",onKeydown);if(closeBtn&&typeof closeBtn.focus==="function"){closeBtn.focus();}}
function onKeydown(event){if(event.key==="Escape"){closeModal();}}
if(fileInput){fileInput.addEventListener("change",()=>{const file=fileInput.files&&fileInput.files.length>0?fileInput.files[0]:null;if(!file){if(fileNameEl){fileNameEl.textContent=fileEmptyText;}
selectedFile=null;setFileError(requiresFile?"Selecciona un archivo.":"");return;}
if(file.size>RECEIPT_MAX_BYTES){selectedFile=null;fileInput.value="";if(fileNameEl){fileNameEl.textContent=fileEmptyText;}
setFileError("El archivo supera los 10 MB permitidos.");return;}
const type=(file.type||"").toLowerCase();const extension=(file.name.split(".").pop()||"").toLowerCase();const allowedType=RECEIPT_ALLOWED_MIMES.includes(type);const allowedExt=RECEIPT_ALLOWED_EXTENSIONS.includes(extension);if(!allowedType&&!allowedExt){selectedFile=null;fileInput.value="";if(fileNameEl){fileNameEl.textContent=fileEmptyText;}
setFileError("Formato no admitido. Usa PDF, JPG o PNG.");return;}
selectedFile=file;if(fileNameEl){fileNameEl.textContent=file.name;}
setFileError("");});}
if(checkboxInput){checkboxInput.addEventListener("change",()=>{updateConfirmState();});}
if(cancelBtn){cancelBtn.addEventListener("click",()=>{closeModal();});}
if(closeBtn){closeBtn.addEventListener("click",()=>{closeModal();});}
modal.addEventListener("click",(event)=>{if(event.target===modal){closeModal();}});if(confirmBtn){confirmBtn.addEventListener("click",()=>{if(confirmBtn.disabled){return;}
const context=pendingConfirmContext;if(context){context.receiptFile=selectedFile||null;}
closeModal();if(context){runConfirmRequest(context);}});}
return{open:openModal,close:closeModal,};}
function buildShareUrl(matricula){if(!matricula){return"";}
const normalized=matricula.replace(/\s+/g,"");const base=misGarantiasBase||"/garantias-online/mis-garantias/";const separator=base.indexOf("?")!==-1?"&":"?";return`${base}${separator}matricula=${encodeURIComponent(normalized)}`;}
function handleShareClick(event){const btn=event.target.closest(".guarantee-detail__btn--share");if(!btn){return;}
const panel=btn.closest(".guarantee-detail__panel");if(!panel){return;}
const matricula=panel.dataset.matricula||"";const shareUrl=buildShareUrl(matricula);if(!shareUrl){return;}
const plan=panel.dataset.plan||"";const title=plan?`Garantía ${matricula} · ${plan}`:`Garantía ${matricula}`;const text=plan?`Consulta la documentación de la garantía ${matricula} (${plan}).`:`Consulta la documentación de la garantía ${matricula}.`;if(navigator.share){navigator.share({title,text,url:shareUrl}).catch((err)=>{if(err&&err.name==="AbortError"){return;}
showDetailToast(panel,SHARE_UNAVAILABLE_MESSAGE);});return;}
showDetailToast(panel,SHARE_UNAVAILABLE_MESSAGE);}
function detailCopyText(text){if(navigator.clipboard&&navigator.clipboard.writeText){return navigator.clipboard.writeText(text);}
const textarea=document.createElement("textarea");textarea.value=text;textarea.setAttribute("readonly","true");textarea.style.position="absolute";textarea.style.left="-9999px";document.body.appendChild(textarea);textarea.select();try{document.execCommand("copy");}catch(err){console.error("Fallback copy failed",err);}
document.body.removeChild(textarea);return Promise.resolve();}
function showDetailToast(panel,message){const toast=panel.querySelector(".detail__copy-toast");if(!toast)return;toast.textContent=message||"Copiado al portapapeles.";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),2000);}
function handleDetailCopy(btn,panel){if(!btn||!panel)return;const selector=btn.dataset.copy;if(!selector)return;const target=panel.querySelector(selector);if(!target)return;const text=target.textContent.trim();if(!text)return;const originalLabel=btn.getAttribute("aria-label")||"";const doneLabel=btn.dataset.done||"Copiado";const toastMessage=btn.dataset.toast||doneLabel;detailCopyText(text).then(()=>{showDetailToast(panel,toastMessage);if(doneLabel){btn.setAttribute("aria-label",doneLabel);setTimeout(()=>{if(originalLabel){btn.setAttribute("aria-label",originalLabel);}},2000);}});}
document.addEventListener("click",(e)=>{const button=e.target.closest(".detail__copy-btn");if(button){const panel=button.closest(".guarantee-detail__panel");if(panel){handleDetailCopy(button,panel);}
return;}
const row=e.target.closest("tr[data-copy-row]");if(row){const panel=row.closest(".guarantee-detail__panel");if(!panel){return;}
const proxyButton=row.querySelector(".detail__copy-btn");if(!proxyButton){return;}
handleDetailCopy(proxyButton,panel);return;}
const cell=e.target.closest("[data-copy-cell]");if(!cell){return;}
const panel=cell.closest(".guarantee-detail__panel");if(!panel){return;}
const proxyButton=cell.querySelector(".detail__copy-btn");if(!proxyButton){return;}
handleDetailCopy(proxyButton,panel);});function normalizeEstadoClase(estado){if(!estado)return"pendiente-pago";return String(estado).toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"");}
function initResizableColumns(table){if(window.innerWidth<1024||!table)return;const wrapper=table.parentElement;wrapper.style.position="relative";table.style.tableLayout="fixed";const ths=Array.from(table.querySelectorAll("thead th"));if(!ths.length)return;const MIN_WIDTH=140;const MAX_WIDTH=300;const clamp=(v,min,max)=>Math.min(Math.max(v,min),max);let colgroup=table.querySelector("colgroup");if(!colgroup){colgroup=document.createElement("colgroup");ths.forEach(()=>colgroup.appendChild(document.createElement("col")));table.insertBefore(colgroup,table.firstChild);}
const cols=Array.from(colgroup.children);let widths=ths.map((th)=>clamp(th.getBoundingClientRect().width,MIN_WIDTH,MAX_WIDTH));widths.forEach((w,i)=>(cols[i].style.width=`${w}px`));const overlay=document.createElement("div");overlay.className="column-resizers";wrapper.appendChild(overlay);const handles=[];function createHandles(){overlay.innerHTML="";handles.length=0;for(let i=0;i<widths.length-1;i++){const h=document.createElement("span");h.className="column-resizer";overlay.appendChild(h);handles.push(h);h.addEventListener("mousedown",(e)=>{e.preventDefault();const startX=e.pageX;const startW=widths[i];const startNext=widths[i+1];function onMove(ev){const dx=ev.pageX-startX;const total=startW+startNext;let newW=clamp(startW+dx,MIN_WIDTH,MAX_WIDTH);let newNext=total-newW;if(newNext<MIN_WIDTH){newNext=MIN_WIDTH;newW=total-newNext;}
if(newNext>MAX_WIDTH){newNext=MAX_WIDTH;newW=total-newNext;}
widths[i]=newW;widths[i+1]=newNext;cols[i].style.width=`${newW}px`;cols[i+1].style.width=`${newNext}px`;updateOverlay();}
function onUp(){document.removeEventListener("mousemove",onMove);document.removeEventListener("mouseup",onUp);}
document.addEventListener("mousemove",onMove);document.addEventListener("mouseup",onUp);});}
updateOverlay();}
function updateOverlay(){overlay.style.width=`${table.offsetWidth}px`;overlay.style.height=`${table.offsetHeight}px`;overlay.style.top=`${table.offsetTop}px`;overlay.style.left=`${table.offsetLeft}px`;handles.forEach((h,i)=>{const th=ths[i];const left=th.offsetLeft+th.offsetWidth;h.style.left=`${left - 4}px`;});}
createHandles();const bodyObserver=new MutationObserver(()=>{widths=ths.map((th)=>clamp(th.getBoundingClientRect().width,MIN_WIDTH,MAX_WIDTH));widths.forEach((w,i)=>(cols[i].style.width=`${w}px`));createHandles();});bodyObserver.observe(table.tBodies[0],{childList:true});window.addEventListener("resize",()=>{updateOverlay();});}
function formatDate(value){if(!value)return{iso:"-",display:"-"};let cleaned=String(value).replace(/[^0-9]/g,"");if(cleaned.length===8){const y=cleaned.slice(0,4);const m=cleaned.slice(4,6);const d=cleaned.slice(6,8);const iso=`${y}-${m}-${d}`;const date=new Date(iso);if(!isNaN(date)){const display=new Intl.DateTimeFormat("es-ES",{day:"2-digit",month:"2-digit",year:"2-digit",}).format(date);return{iso,display};}
return{iso,display:`${d}/${m}/${y.slice(2)}`};}
const date=new Date(value);if(!isNaN(date)){const year=date.getFullYear();const month=String(date.getMonth()+1).padStart(2,"0");const day=String(date.getDate()).padStart(2,"0");const iso=`${year}-${month}-${day}`;const display=new Intl.DateTimeFormat("es-ES",{day:"2-digit",month:"2-digit",year:"2-digit",}).format(date);return{iso,display};}
return{iso:value,display:value};}
function formatPrice(value){if(value===null||value===undefined||value==="")return"-";const num=typeof value==="number"?value:parseFloat(String(value).replace(/[^0-9.,-]/g,"").replace(",","."));if(isNaN(num))return String(value);return new Intl.NumberFormat("es-ES",{minimumFractionDigits:2,maximumFractionDigits:2,}).format(num);}
function extractVendorType(value){if(value===null||value===undefined){return"";}
const label=String(value).trim();if(!label){return"";}
const match=label.match(/\(([^)]+)\)/);if(match&&match[1]){return match[1].trim();}
const cleaned=label.replace(/^Profesional\s*[-–:|]?\s*/i,"").trim();return cleaned||label;}
const{getTransferDeadlineMillis,getTransferDeadlineInfo}=(()=>{function parseDateTime(value){if(!value)return null;if(value instanceof Date)return value;if(typeof value==="number"){const fromNumber=new Date(value);if(!Number.isNaN(fromNumber.getTime())){return fromNumber;}}
const normalized=String(value).trim();if(!normalized)return null;const safeDate=(year,month,day,hours=0,minutes=0,seconds=0)=>{const y=Number(year);const m=Number(month)-1;const d=Number(day);const hh=Number(hours);const mm=Number(minutes);const ss=Number(seconds);const candidate=new Date(y,m,d,hh,mm,ss);if(Number.isNaN(candidate.getTime()))return null;return candidate;};const localMatch=normalized.match(/^(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})(?:[ T](\d{1,2}):(\d{2})(?::(\d{2}))?)?$/);if(localMatch){const[,day,month,year,hours="0",minutes="0",seconds="0"]=localMatch;const date=safeDate(year,month,day,hours,minutes,seconds);if(date)return date;}
const isoMatch=normalized.match(/^(\d{4})[\/\-](\d{1,2})[\/\-](\d{1,2})(?:[ T](\d{1,2}):(\d{2})(?::(\d{2}))?)?$/);if(isoMatch){const[,year,month,day,hours="0",minutes="0",seconds="0"]=isoMatch;const date=safeDate(year,month,day,hours,minutes,seconds);if(date)return date;}
const numeric=normalized.replace(/[^0-9]/g,"");const parseFromNumeric=(year,month,day,hours="0",minutes="0",seconds="0")=>safeDate(year,month,day,hours,minutes,seconds);if(numeric.length>=14){const firstChunk=Number(numeric.slice(0,4));if(firstChunk>1900){const date=parseFromNumeric(numeric.slice(0,4),numeric.slice(4,6),numeric.slice(6,8),numeric.slice(8,10),numeric.slice(10,12),numeric.slice(12,14));if(date)return date;}else{const date=parseFromNumeric(numeric.slice(4,8),numeric.slice(2,4),numeric.slice(0,2),numeric.slice(8,10),numeric.slice(10,12),numeric.slice(12,14));if(date)return date;}}
if(numeric.length===12){const firstChunk=Number(numeric.slice(0,4));if(firstChunk>1900){const date=parseFromNumeric(numeric.slice(0,4),numeric.slice(4,6),numeric.slice(6,8),numeric.slice(8,10),numeric.slice(10,12));if(date)return date;}else{const date=parseFromNumeric(numeric.slice(4,8),numeric.slice(2,4),numeric.slice(0,2),numeric.slice(8,10),numeric.slice(10,12));if(date)return date;}}
if(numeric.length===8){const firstChunk=Number(numeric.slice(0,4));if(firstChunk>1900){const date=parseFromNumeric(numeric.slice(0,4),numeric.slice(4,6),numeric.slice(6,8));if(date)return date;}else{const date=parseFromNumeric(numeric.slice(4,8),numeric.slice(2,4),numeric.slice(0,2));if(date)return date;}}
const parsed=new Date(normalized);if(!Number.isNaN(parsed.getTime())){return parsed;}
return null;}
function isSameCalendarDay(a,b){return(a.getFullYear()===b.getFullYear()&&a.getMonth()===b.getMonth()&&a.getDate()===b.getDate());}
function getTransferDeadlineMillis(detailData,rowData,now=Date.now()){const candidates=[detailData?.desde_raw,rowData?.desde_raw,detailData?.desde,rowData?.desde,];for(const candidate of candidates){const date=parseDateTime(candidate);if(date){const nowDate=new Date(now);if(isSameCalendarDay(date,nowDate)){return now+48*60*60*1000;}
return date.getTime()+48*60*60*1000;}}
return null;}
function formatDeadlineDate(deadlineMs){if(!Number.isFinite(deadlineMs)){return"";}
const date=new Date(deadlineMs);if(Number.isNaN(date.getTime())){return"";}
try{return new Intl.DateTimeFormat("es-ES",{day:"numeric",month:"long",}).format(date);}catch(error){console.warn("No se pudo formatear la fecha límite de transferencia",error);return"";}}
function getTransferDeadlineInfo(detailData,rowData,now=Date.now()){const deadlineMs=getTransferDeadlineMillis(detailData,rowData,now);if(!Number.isFinite(deadlineMs)){return{deadlineMs:null,expired:false,label:"",};}
return{deadlineMs,expired:deadlineMs<=now,label:formatDeadlineDate(deadlineMs),};}
return{getTransferDeadlineMillis,getTransferDeadlineInfo};})();function clearActiveCountdown(){}
function setupTransferCountdown(){}
function normalizeDetailData(data){if(!data||typeof data!=="object")return data;const rawDesde=data.desde;const d=formatDate(rawDesde);data.desde=d.iso;data.desde_fmt=d.display;data.desde_raw=rawDesde||"";const rawHasta=data.hasta;const h=formatDate(rawHasta);data.hasta=h.iso;data.hasta_fmt=h.display;data.hasta_raw=rawHasta||"";if(data.precio!==undefined)data.precio=formatPrice(data.precio);if(data.precio_venta!==undefined)
data.precio_venta=formatPrice(data.precio_venta);if(data.kilometros!==undefined){const num=parseInt(String(data.kilometros).replace(/[^0-9]/g,""),10);if(!isNaN(num)){data.kilometros=num.toLocaleString("es-ES");}}
if(data.cilindrada===undefined&&data.Cilindrada!==undefined){data.cilindrada=data.Cilindrada;}
if(data.cilindrada!==undefined){const num=parseInt(String(data.cilindrada).replace(/[^0-9]/g,""),10);if(!isNaN(num)){data.cilindrada=num.toLocaleString("es-ES");}}
if(data.potencia!==undefined){const num=parseInt(String(data.potencia).replace(/[^0-9]/g,""),10);if(!isNaN(num)){data.potencia=num.toLocaleString("es-ES");}}
if(data.combustible&&typeof data.combustible==="object"){data.combustible=data.combustible.label||data.combustible.name||data.combustible.value||data.combustible;}
if(data.cambio&&typeof data.cambio==="object"){data.cambio=data.cambio.label||data.cambio.name||data.cambio.value||data.cambio;}
if(data.tipo&&typeof data.tipo==="object"){data.tipo=data.tipo.label||data.tipo.name||data.tipo.value||data.tipo;}
if(typeof data.transfer_iban==="string"){data.transfer_iban=data.transfer_iban.trim();}
if(!Array.isArray(data.documents)){data.documents=[];}
return data;}
function renderRow(item){const estadoData=item.estado||"";const estadoValue=typeof estadoData==="object"&&estadoData.value?estadoData.value:typeof estadoData==="string"&&estadoData?estadoData:typeof estadoData==="number"?String(estadoData):"";const estadoLabel=typeof estadoData==="object"&&estadoData.label?estadoData.label:estadoValue||"Desconocido";const estadoClase=normalizeEstadoClase(estadoValue);const marca_modelo=item.marca??"-";const mat=item.mat??item.matricula??"-";const rawDesde=item.desde??"";const rawHasta=item.hasta??"";const{iso:desdeIso,display:desde}=formatDate(rawDesde);const{iso:hastaIso,display:hasta}=formatDate(rawHasta);const vendedor_name=item.vendedor??"-";const plan=item.plan??"";const precio=formatPrice(item.precio);const canal_venta=item.canal_venta&&item.canal_venta.label?item.canal_venta.label:"-";const canal_venta_summary=(item.detail&&item.detail.canal_venta_summary)?item.detail.canal_venta_summary:canal_venta;const vendedor_type_raw=canal_venta_summary;const vendedor_type_clean=extractVendorType(vendedor_type_raw);const vendedor_type=vendedor_type_clean||vendedor_type_raw||"-";const cliente_nombre=item.detail?.nombre_comprador??"-";const cliente_telefono_raw=item.detail?.telefono_comprador??"";const cliente_telefono=cliente_telefono_raw&&cliente_telefono_raw!=="-"?cliente_telefono_raw:"";const cliente_telefono_dataset=cliente_telefono!==""?cliente_telefono:"-";const hasPlan=plan!==""&&plan!=="-";const hasPeriod=desde!=="-"&&hasta!=="-"&&desde!==""&&hasta!=="";const periodHtml=hasPeriod?`<div class="guarantees-table__period">
                                                <div><strong>Desde:</strong> <time>${desde}</time></div>
                                                <div><strong>Hasta:</strong> <time>${hasta}</time></div>
                                        </div>`:"-";const planHtml=hasPlan?`<div class="guarantees-table__plan">
                                                <span class="plan__name">${plan}</span>
                                                <span class="plan__price">${precio}€</span>
                                        </div>`:"";const clientePhoneHtml=cliente_telefono!==""?`<div class="cliente__phone">${cliente_telefono}</div>`:"";const thirdColumnLabel=isProfesional?"Cliente":"Canal de venta";const thirdColumnHtml=isProfesional?`<div class="guarantees-table__cliente">
                                <div class="cliente__name">${cliente_nombre}</div>
                                ${clientePhoneHtml}
                        </div>`:`<div class="guarantees-table__vendedor">
                                <div class="vendedor__name">${vendedor_name}</div>
                                <div class="vendedor__type">${vendedor_type}</div>
                        </div>`;const tr=document.createElement("tr");tr.className="guarantees-table__row";tr.tabIndex=0;tr.dataset.id=item.id;tr.dataset.matricula=mat;tr.dataset.marca_modelo=marca_modelo;tr.dataset.plan=hasPlan?plan:"";tr.dataset.desde=hasPeriod?desdeIso:"";tr.dataset.desdeRaw=hasPeriod?rawDesde:"";tr.dataset.desdeFmt=hasPeriod?desde:"";tr.dataset.hasta=hasPeriod?hastaIso:"";tr.dataset.hastaRaw=hasPeriod?rawHasta:"";tr.dataset.hastaFmt=hasPeriod?hasta:"";tr.dataset.estado=estadoLabel;tr.dataset.estadoclase=estadoClase;tr.dataset.vendedor_name=vendedor_name;tr.dataset.vendedor_type=vendedor_type;tr.dataset.canal_venta_summary=canal_venta_summary;tr.dataset.concesionario=vendedor_name;tr.dataset.concesionario_personal=item.detail?.concesionario_personal??"";tr.dataset.precio=hasPlan?precio:"";tr.dataset.canalVenta=canal_venta;tr.dataset.metodoPago=item.detail.metodo_pago||"";tr.dataset.cobroRealizado=item.detail.cobro_realizado?"1":"";tr.dataset.ibanVendedor=item.detail.iban_vendedor||"";tr.dataset.transferIban=item.detail.transfer_iban||"";tr.dataset.compradorNombre=cliente_nombre||"-";tr.dataset.compradorTelefono=cliente_telefono_dataset;const cobroBadgeHtml=isAdmin&&tr.dataset.metodoPago&&tr.dataset.metodoPago.toLowerCase().startsWith("domiciliacion")&&!tr.dataset.cobroRealizado?`<span class="guarantees-list__badge guarantees-list__badge--pend-cobro">Pend. Domiciliación</span>`:"";const estadoBadgeHtml=`<span class="guarantees-list__badge guarantees-list__badge--${estadoClase}">${estadoLabel}</span>`;tr.innerHTML=`
                                <td data-label="Vehículo">
                                        <div class="guarantees-table__vehiculo">
                                                <div class="vehiculo__mat">${mat}</div>
                                                <div class="vehiculo__marca_modelo">${marca_modelo}</div>
                                        </div>
                                </td>
                                <td data-label="Validez">${periodHtml}</td>
                                <td data-label="${thirdColumnLabel}">${thirdColumnHtml}</td>
                                <td data-label="Estado">
                                        <div class="guarantees-table__estado">
                                                ${estadoBadgeHtml}
                                                ${cobroBadgeHtml}
                                        </div>
                                </td>
                                <td data-label="Garantía">${planHtml}</td>
                        `;const headerCells=table.querySelectorAll("thead th");headerCells.forEach((th,i)=>{const width=th.getBoundingClientRect().width;if(tr.children[i]){tr.children[i].style.width=`${width}px`;}});return tr;}
function renderEmptyRow(){const tr=document.createElement("tr");tr.className="guarantees-table__empty-row";const td=document.createElement("td");const headerCount=table?table.querySelectorAll("thead th").length:1;td.colSpan=Math.max(1,headerCount);td.innerHTML='<div class="guarantees-table__empty-message"><p>Todavía no hay garantías.</p></div>';tr.appendChild(td);return tr;}
function normalizeToFloat(value){if(value===null||value===undefined){return 0;}
if(typeof value==="number"){return Number.isFinite(value)?value:0;}
const normalized=String(value).trim();if(!normalized){return 0;}
const numeric=normalized.replace(/[^0-9,.-]/g,"").replace(/\./g,"").replace(/,/g,".");const parsed=Number(numeric);return Number.isFinite(parsed)?parsed:0;}
function normalizeToInt(value){const number=normalizeToFloat(value);return Math.max(0,Math.round(number));}
function formatIntegerValue(value){return integerFormatter.format(normalizeToInt(value));}
function formatCurrencyValue(value){return currencyFormatter.format(normalizeToFloat(value));}
function formatGuaranteeCount(value){const count=normalizeToInt(value);const formatted=formatIntegerValue(count);return count===1?`${formatted} garantía`:`${formatted} garantías`;}
function animateSummaryNumber(element,target,options={}){if(!element){return;}
const duration=typeof options.duration==="number"?options.duration:500;const startValue=typeof options.start==="number"?options.start:normalizeToInt(element.dataset.value||element.textContent||0);const normalizedTarget=normalizeToInt(target);if(numberAnimations.has(element)){cancelAnimationFrame(numberAnimations.get(element));}
const startTime=performance.now();function step(now){const progress=Math.min((now-startTime)/duration,1);const current=Math.round(startValue+(normalizedTarget-startValue)*progress);element.textContent=formatIntegerValue(current);if(progress<1){numberAnimations.set(element,requestAnimationFrame(step));}else{element.textContent=formatIntegerValue(normalizedTarget);element.dataset.value=String(normalizedTarget);numberAnimations.delete(element);}}
numberAnimations.set(element,requestAnimationFrame(step));}
function setAdminSummaryLoading(root,isLoading){if(!root){return;}
const loading=Boolean(isLoading);root.classList.toggle("is-loading",loading);root.dataset.loading=loading?"1":"0";if(loading){root.dataset.loaded="0";root._adminSummaryData=null;root._adminSummaryContextData=null;root._adminSummaryHighlight=null;root._adminSummaryLockedHighlight=null;root.classList.remove("is-dimmed");}
const error=root.querySelector("[data-admin-summary-error]");if(error){error.hidden=true;}
if(loading){root.dataset.context=ADMIN_SUMMARY_DEFAULT_CONTEXT;setVisualizationHighlight(root,null);const statesList=root.querySelector("[data-admin-summary-states]");if(statesList){statesList.innerHTML="";for(let index=0;index<4;index+=1){const skeleton=document.createElement("div");skeleton.className="legend-item is-loading";statesList.appendChild(skeleton);}}
const actionsList=root.querySelector("[data-admin-summary-actions]");if(actionsList){actionsList.innerHTML="";for(let index=0;index<ADMIN_SUMMARY_ACTIONS.length;index+=1){const skeleton=document.createElement("li");skeleton.className="action-item is-loading";actionsList.appendChild(skeleton);}}
const totalEl=root.querySelector("[data-admin-summary-total]");if(totalEl){totalEl.textContent="—";totalEl.dataset.value="0";totalEl.classList.remove("highlighted");totalEl.style.color="";}
const labelEl=root.querySelector("[data-admin-summary-label]");if(labelEl){labelEl.textContent=labelEl.getAttribute("data-default-label")||"Total";labelEl.dataset.contextLabel="";labelEl.classList.remove("highlighted");labelEl.style.color="";}
const donut=root.querySelector("[data-admin-summary-donut]");if(donut){resetDonutChart(donut);donut.classList.remove("is-empty");}}}
function showAdminSummaryError(root){if(!root){return;}
root.classList.remove("is-loading");root.dataset.loading="0";root.dataset.loaded="0";root.classList.remove("is-dimmed");root._adminSummaryHighlight=null;root._adminSummaryLockedHighlight=null;const legend=root.querySelector("[data-admin-summary-states]");if(legend){legend.innerHTML="";const item=document.createElement("div");item.className="legend-item legend-item--empty";item.textContent=ADMIN_SUMMARY_ERROR_MESSAGE;legend.appendChild(item);}
setVisualizationHighlight(root,null);const actionsList=root.querySelector("[data-admin-summary-actions]");if(actionsList){actionsList.innerHTML="";}
const totalEl=root.querySelector("[data-admin-summary-total]");if(totalEl){totalEl.textContent="—";totalEl.dataset.value="0";totalEl.classList.remove("highlighted");totalEl.style.color="";}
const labelEl=root.querySelector("[data-admin-summary-label]");if(labelEl){labelEl.textContent=labelEl.getAttribute("data-default-label")||"Total";labelEl.dataset.contextLabel="";labelEl.classList.remove("highlighted");labelEl.style.color="";}
const donut=root.querySelector("[data-admin-summary-donut]");if(donut){resetDonutChart(donut);}
const error=root.querySelector("[data-admin-summary-error]");if(error){error.hidden=false;}}
function normalizeSummaryStates(states){if(!Array.isArray(states)){return[];}
const items=states.map((entry)=>{const value=typeof entry.value==="string"?entry.value:"";if(!value){return null;}
const label=entry.label||value;const count=Math.max(0,normalizeToInt(entry.count||0));return{value,label,count};}).filter(Boolean);if(items.length<=1){return items;}
const orderMap=new Map(ADMIN_SUMMARY_STATE_ORDER.map((key,index)=>[key,index]));items.sort((a,b)=>{const orderA=orderMap.has(a.value)?orderMap.get(a.value):99;const orderB=orderMap.has(b.value)?orderMap.get(b.value):99;if(orderA!==orderB){return orderA-orderB;}
if(b.count!==a.count){return b.count-a.count;}
return a.label.localeCompare(b.label);});return items;}
function resetDonutChart(donut){if(!donut){return;}
donut._adminSummarySegments=[];donut._adminSummaryTotal=0;donut.style.setProperty("--segment-0-end","0%");for(let index=1;index<=4;index+=1){donut.style.setProperty(`--segment-${index}-end`,index===4?"100%":"0%");donut.style.setProperty(`--segment-${index}-color`,"transparent");}
donut.style.setProperty("--p","0");donut.classList.add("is-empty");donut.classList.remove("is-highlighted");ADMIN_SUMMARY_STATE_VALUES.forEach((value)=>{donut.classList.remove(`dimmed-${value}`);});donut.style.removeProperty("--donut-highlight-color");donut.style.removeProperty("--donut-highlight-muted");}
function configureDonutSegments(donut,items){if(!donut){return;}
const positive=Array.isArray(items)?items.filter((item)=>item.count>0):[];const total=positive.reduce((sum,entry)=>sum+entry.count,0);donut._adminSummarySegments=[];donut._adminSummaryTotal=total;if(positive.length===0||total<=0){resetDonutChart(donut);return;}
donut.classList.remove("is-empty");donut.style.setProperty("--segment-0-end","0%");let cumulative=0;positive.forEach((item,index)=>{const palette=ADMIN_SUMMARY_STATE_COLORS[item.value]||{};cumulative+=item.count/total;const endValue=index===positive.length-1?100:Math.min(100,Math.max(0,cumulative*100));donut.style.setProperty(`--segment-${index + 1}-end`,`${endValue.toFixed(2)}%`);donut._adminSummarySegments.push({value:item.value,color:palette.color||"transparent",muted:palette.muted||palette.color||"transparent",});});for(let index=positive.length+1;index<=4;index+=1){donut.style.setProperty(`--segment-${index}-end`,"100%");}
updateDonutColors(donut,null);playDonutAnimation(donut);}
function playDonutAnimation(donut){if(!donut){return;}
donut.style.setProperty("--p","0");void donut.offsetWidth;requestAnimationFrame(()=>{donut.style.setProperty("--p","1");});}
function scheduleDonutReplay(root,{immediate=false}={}){if(!root){return;}
const donut=root.querySelector?root.querySelector("[data-admin-summary-donut]"):null;if(!donut){return;}
const trigger=()=>playDonutAnimation(donut);if(immediate){trigger();return;}
requestAnimationFrame(()=>{requestAnimationFrame(trigger);});}
function updateDonutColors(donut,highlightValue){if(!donut){return;}
const segments=Array.isArray(donut._adminSummarySegments)?donut._adminSummarySegments:[];if(segments.length===0){for(let index=1;index<=4;index+=1){donut.style.setProperty(`--segment-${index}-color`,DONUT_BASE_COLOR_TOKEN);}
ADMIN_SUMMARY_STATE_VALUES.forEach((value)=>{donut.classList.remove(`dimmed-${value}`);});donut.classList.remove("is-highlighted");donut.style.removeProperty("--donut-highlight-color");donut.style.removeProperty("--donut-highlight-muted");return;}
const highlightSegment=highlightValue?segments.find((segment)=>segment.value===highlightValue):null;const highlightColor=highlightSegment?highlightSegment.color:null;const highlightMuted=highlightSegment?highlightSegment.muted||highlightSegment.color:null;ADMIN_SUMMARY_STATE_VALUES.forEach((value)=>{donut.classList.toggle(`dimmed-${value}`,Boolean(highlightSegment)&&value===highlightValue);});donut.classList.toggle("is-highlighted",Boolean(highlightSegment));if(highlightSegment){donut.style.setProperty("--donut-highlight-color",highlightColor||"transparent");donut.style.setProperty("--donut-highlight-muted",highlightMuted||highlightColor||"transparent");}else{donut.style.removeProperty("--donut-highlight-color");donut.style.removeProperty("--donut-highlight-muted");}
segments.forEach((segment,index)=>{let color=segment.color;if(highlightSegment){color=segment.value===highlightValue?highlightColor:highlightMuted;}
donut.style.setProperty(`--segment-${index + 1}-color`,color||"transparent");});const fallbackColor=highlightSegment?highlightMuted||highlightColor||"transparent":DONUT_BASE_COLOR_TOKEN;for(let index=segments.length+1;index<=4;index+=1){donut.style.setProperty(`--segment-${index}-color`,fallbackColor);}}
function setVisualizationHighlight(root,highlightValue){if(!root){return;}
const visualization=root.querySelector(".visualization-section");if(!visualization){return;}
if(highlightValue){visualization.classList.add("is-dimmed");visualization.dataset.highlight=highlightValue;}else{visualization.classList.remove("is-dimmed");delete visualization.dataset.highlight;}}
function sanitizeLegendState(root,value){if(!root||!value){return null;}
const contextData=root._adminSummaryContextData||{};const items=Array.isArray(contextData.items)?contextData.items:[];const match=items.find((entry)=>entry.value===value);return match&&match.value?match.value:null;}
function renderAdminSummaryStates(root,context={}){const donut=root.querySelector("[data-admin-summary-donut]");const legend=root.querySelector("[data-admin-summary-states]");const totalEl=root.querySelector("[data-admin-summary-total]");const labelEl=root.querySelector("[data-admin-summary-label]");const items=normalizeSummaryStates((context&&context.states)||[]);const total=items.reduce((sum,item)=>sum+item.count,0);const defaultLabel=labelEl?labelEl.getAttribute("data-default-label")||"Total":"Total";const contextLabel=context&&typeof context.label==="string"?context.label.trim():"";if(labelEl){const effectiveLabel=contextLabel||defaultLabel;labelEl.textContent=effectiveLabel;labelEl.dataset.contextLabel=effectiveLabel;labelEl.classList.remove("highlighted");labelEl.style.color="";}
if(totalEl){if(typeof totalEl.dataset.value==="undefined"){totalEl.dataset.value="0";}
totalEl.classList.remove("highlighted");totalEl.style.color="";}
if(donut){configureDonutSegments(donut,items);}
if(!legend){return;}
legend.innerHTML="";if(items.length===0){const item=document.createElement("div");item.className="legend-item legend-item--empty";item.textContent="Sin datos disponibles";legend.appendChild(item);root._adminSummaryContextData={items,total,label:contextLabel||defaultLabel,};root._adminSummaryHighlight=null;root._adminSummaryLockedHighlight=null;applyLegendHighlight(root,null);return;}
items.forEach((item)=>{const percent=total>0?Math.round((item.count/total)*100):0;const palette=ADMIN_SUMMARY_STATE_COLORS[item.value]||{};const node=document.createElement("div");node.className="legend-item";if(item.value){node.dataset.state=item.value;}
const color=palette.color||"var(--admin-summary-state-activada)";node.innerHTML=`
                <span class="legend-color" style="background-color: ${color}"></span>
                <div class="legend-info">
                    <span class="legend-label">${escapeHtml(item.label)}</span>
                    <span class="legend-value">${formatIntegerValue(percent)}%</span>
                </div>
            `;node.tabIndex=0;legend.appendChild(node);});const legendItems=legend.querySelectorAll(".legend-item");legendItems.forEach((node)=>{const handleEnter=()=>{const state=node.dataset.state||null;applyLegendHighlight(root,state);};const handleLeave=()=>{const locked=root._adminSummaryLockedHighlight||null;applyLegendHighlight(root,locked);};node.addEventListener("mouseenter",handleEnter);node.addEventListener("focus",handleEnter);node.addEventListener("mouseleave",handleLeave);node.addEventListener("blur",handleLeave);node.addEventListener("click",(event)=>{event.preventDefault();const state=node.dataset.state||null;const locked=root._adminSummaryLockedHighlight||null;const next=locked===state?null:state;root._adminSummaryLockedHighlight=sanitizeLegendState(root,next);applyLegendHighlight(root,root._adminSummaryLockedHighlight||null);});node.addEventListener("keydown",(event)=>{if(event.key==="Enter"||event.key===" "){event.preventDefault();node.click();}});});if(!legend._adminSummaryLeaveBound){legend.addEventListener("mouseleave",()=>{const locked=root._adminSummaryLockedHighlight||null;applyLegendHighlight(root,locked);});legend._adminSummaryLeaveBound=true;}
root._adminSummaryContextData={items,total,label:contextLabel||defaultLabel,};root._adminSummaryLockedHighlight=sanitizeLegendState(root,root._adminSummaryLockedHighlight||null);root._adminSummaryHighlight=null;applyLegendHighlight(root,root._adminSummaryLockedHighlight||null);scheduleDonutReplay(root,{immediate:true});}
function applyLegendHighlight(root,stateValue=null){if(!root){return;}
const donut=root.querySelector("[data-admin-summary-donut]");const totalEl=root.querySelector("[data-admin-summary-total]");const labelEl=root.querySelector("[data-admin-summary-label]");const legend=root.querySelector("[data-admin-summary-states]");const contextData=root._adminSummaryContextData||{};const items=Array.isArray(contextData.items)?contextData.items:[];const total=typeof contextData.total==="number"?contextData.total:items.reduce((sum,item)=>sum+(item.count||0),0);const defaultLabel=labelEl?labelEl.getAttribute("data-default-label")||"Total":"Total";const baseLabel=contextData.label||defaultLabel;const sanitizedValue=sanitizeLegendState(root,stateValue);const highlightItem=sanitizedValue?items.find((entry)=>entry.value===sanitizedValue):null;const effectiveHighlight=highlightItem&&highlightItem.value?highlightItem.value:null;root._adminSummaryHighlight=effectiveHighlight;if(!effectiveHighlight){root._adminSummaryLockedHighlight=sanitizeLegendState(root,root._adminSummaryLockedHighlight||null);}
if(legend){const nodes=legend.querySelectorAll(".legend-item");nodes.forEach((node)=>{const isActive=Boolean(effectiveHighlight)&&node.dataset.state===effectiveHighlight;node.classList.toggle("is-highlighted",isActive);});}
setVisualizationHighlight(root,effectiveHighlight);if(totalEl){const targetTotal=effectiveHighlight&&highlightItem?highlightItem.count||0:total;animateSummaryNumber(totalEl,targetTotal,{duration:effectiveHighlight?220:320,});if(effectiveHighlight&&highlightItem){const palette=ADMIN_SUMMARY_STATE_COLORS[highlightItem.value]||{};totalEl.classList.add("highlighted");totalEl.style.color=palette.color||"";}else{totalEl.classList.remove("highlighted");totalEl.style.color="";}}
if(labelEl){if(effectiveHighlight&&highlightItem){const palette=ADMIN_SUMMARY_STATE_COLORS[highlightItem.value]||{};labelEl.textContent=highlightItem.label||baseLabel;labelEl.classList.add("highlighted");labelEl.style.color=palette.color||"";}else{labelEl.textContent=baseLabel;labelEl.classList.remove("highlighted");labelEl.style.color="";}}
if(!donut){return;}
if(items.length===0||total===0){resetDonutChart(donut);return;}
updateDonutColors(donut,effectiveHighlight);}
function getSummaryContextData(data,contextKey){if(!data||typeof data!=="object"){return{states:[]};}
const contexts=data.contexts&&typeof data.contexts==="object"?data.contexts:{};if(contextKey&&contexts[contextKey]){return contexts[contextKey];}
if(contexts[ADMIN_SUMMARY_DEFAULT_CONTEXT]){return contexts[ADMIN_SUMMARY_DEFAULT_CONTEXT];}
const firstContext=Object.values(contexts)[0];if(firstContext){return firstContext;}
return{states:data.states||[],label:"",count:data.totals&&data.totals.count?data.totals.count:0,};}
function updateAdminSummaryContext(root,contextKey){if(!root){return;}
const data=root._adminSummaryData||{};const contextData=getSummaryContextData(data,contextKey);const effectiveKey=contextKey&&contextData?contextKey:ADMIN_SUMMARY_DEFAULT_CONTEXT;root.dataset.context=effectiveKey;const toggles=root.querySelectorAll("[data-admin-summary-context-toggle]");toggles.forEach((toggle)=>{const value=toggle.value||toggle.getAttribute("value");toggle.checked=value===effectiveKey;});renderAdminSummaryStates(root,contextData||{});scheduleDonutReplay(root,{immediate:true});}
function renderAdminSummaryActions(root,pending={}){const list=root.querySelector("[data-admin-summary-actions]");if(!list){return;}
list.innerHTML="";ADMIN_SUMMARY_ACTIONS.forEach((action)=>{const entry=pending[action.key]||{};const amount=normalizeToFloat(entry.amount||0);const count=normalizeToInt(entry.count||0);const item=document.createElement("li");item.className="action-item";item.dataset.action=action.key;if(action.filterValue){item.dataset.filterValue=action.filterValue;}
if(action.accent){item.style.setProperty("--action-accent",action.accent);}
const amountLabel=formatCurrencyValue(amount);const countLabel=formatGuaranteeCount(count);const subtitle=count>0?`${countLabel} · ${amountLabel}`:"Sin pendientes";item.innerHTML=`
                <span class="action-icon" aria-hidden="true" style="background-color: var(--action-accent)">
                    ${action.icon}
                </span>
                <div class="action-details">
                    <span class="action-label">${escapeHtml(action.label)}</span>
                    <span class="action-sublabel">${escapeHtml(subtitle)}</span>
                </div>
                <span class="action-cta" aria-hidden="true">${ADMIN_SUMMARY_ACTION_ARROW_ICON}</span>
            `;if(count===0){item.classList.add("is-empty");}
item.addEventListener("click",()=>{if(!action.filterValue||!estadoSelect){return;}
if(estadoSelect.value===action.filterValue){applyFilters();return;}
estadoSelect.value=action.filterValue;selectedEstado=action.filterValue;updateResetVisibility();applyFilters();});list.appendChild(item);});}
function renderAdminSummary(root,data={}){if(!root){return;}
root.classList.remove("is-loading");root.dataset.loading="0";root.dataset.loaded="1";root._adminSummaryData=data||{};const currentContext=root.dataset.context||ADMIN_SUMMARY_DEFAULT_CONTEXT;updateAdminSummaryContext(root,currentContext);renderAdminSummaryActions(root,data.pending||{});const error=root.querySelector("[data-admin-summary-error]");if(error){error.hidden=true;}}
function fetchAdminSummary(force=false){if(!isCoreAdmin){return Promise.resolve({});}
if(force){adminSummaryCache=null;}else{if(adminSummaryCache){return Promise.resolve(adminSummaryCache);}
if(adminSummaryPromise){return adminSummaryPromise;}}
const headers={Accept:"application/json"};if(restNonce){headers["X-WP-Nonce"]=restNonce;}
const request=fetch(`${restRoot}go/v1/guarantees/summary`,{headers}).then((response)=>{if(!response.ok){throw new Error(`Resumen de garantías no disponible (${response.status || ""})`);}
return response.json();}).then((payload)=>{adminSummaryCache=payload;return payload;}).finally(()=>{adminSummaryPromise=null;});if(!force){adminSummaryPromise=request;}
return request;}
function loadAdminSummary(root,options={}){if(!root){return;}
const force=Boolean(options.force);if(!force&&root.dataset.loaded==="1"&&adminSummaryCache){renderAdminSummary(root,adminSummaryCache);return;}
setAdminSummaryLoading(root,true);fetchAdminSummary(force).then((payload)=>{renderAdminSummary(root,payload||{});}).catch((error)=>{console.error(error);root.dataset.loaded="0";root.classList.remove("is-loading");root.dataset.loading="0";showAdminSummaryError(root);});}
function initializeAdminSummary(panel){if(!isCoreAdmin||!panel){return;}
const root=panel.querySelector("[data-admin-summary]");if(!root){return;}
if(root.dataset.initialized==="1"&&root.dataset.loaded==="1"&&adminSummaryCache){renderAdminSummary(root,adminSummaryCache);return;}
const toggles=root.querySelectorAll("[data-admin-summary-context-toggle]");toggles.forEach((toggle)=>{toggle.addEventListener("change",()=>{if(!toggle.checked){return;}
updateAdminSummaryContext(root,toggle.value||toggle.getAttribute("value"));});});const helpButton=root.querySelector("[data-admin-summary-help]");if(helpButton){helpButton.addEventListener("click",()=>{const event=new CustomEvent("go:summary-help",{bubbles:true,detail:{source:"admin-summary"},});root.dispatchEvent(event);});}
const preloadNode=root.querySelector("[data-admin-summary-preload]");let preloadedData=null;if(preloadNode){try{preloadedData=JSON.parse(preloadNode.textContent||"{}");}catch(error){console.error("No se pudo analizar el resumen precargado",error);}
preloadNode.remove();}
root.dataset.initialized="1";if(!root.dataset.context){root.dataset.context=ADMIN_SUMMARY_DEFAULT_CONTEXT;}
if(preloadedData){adminSummaryCache=preloadedData;renderAdminSummary(root,preloadedData||{});}else{loadAdminSummary(root);}}
function renderEmptyDetail(mode="awaiting"){const templateKey=mode==="no-results"?"no-results":"awaiting";if(emptyTemplateMap[templateKey]){return emptyTemplateMap[templateKey];}
if(templateKey==="no-results"){const safeUrl=typeof newGuaranteeUrl==="string"&&newGuaranteeUrl?newGuaranteeUrl:"#";const iconHtml=plusIcon?`<span class="guarantee-detail__cta-icon" aria-hidden="true">${plusIcon}</span>`:"";return`
                                        <div class="guarantee-detail__empty" data-empty-detail data-empty-mode="no-results">
                                                <h3 class="guarantee-detail__title">Añade tu primera garantía</h3>
                                                <p>Crea una nueva garantía para ver aquí todos sus detalles.</p>
                                                <a class="guarantee-detail__cta" href="${safeUrl}">
                                                        ${iconHtml}
                                                        <span class="guarantee-detail__cta-label">Nueva Garantía</span>
                                                </a>
                                        </div>
                                `;}
const arrowHtml=arrowLeftIcon?`<span class="guarantee-detail__hint-arrow" aria-hidden="true">${arrowLeftIcon}</span>`:'<span class="guarantee-detail__hint-arrow" aria-hidden="true"></span>';return`
                                <div class="guarantee-detail__empty" data-empty-detail data-empty-mode="awaiting">
                                        <h3 class="guarantee-detail__title">Consulta los detalles de tus garantías</h3>
                                        <p class="guarantee-detail__hint">
                                                ${arrowHtml}
                                                Haz clic en una garantía para consultar la información completa.
                                        </p>
                                </div>
                        `;}
function setEmptyDetailPanel(direction="forward",mode="awaiting"){clearActiveCountdown();const currentActive=activePanel;const nextPanel=activePanel===panel1?panel2:panel1;const normalizedMode=mode==="no-results"?"no-results":"awaiting";const activeHasSameMode=normalizedMode===currentEmptyMode&&currentActive&&currentActive.classList.contains("active")&&currentActive.querySelector(`[data-empty-detail][data-empty-mode="${normalizedMode}"]`);const nextHasSameMode=normalizedMode===currentEmptyMode&&nextPanel.classList.contains("active")&&nextPanel.querySelector(`[data-empty-detail][data-empty-mode="${normalizedMode}"]`);if(activeHasSameMode||nextHasSameMode){return;}
nextPanel.innerHTML=renderEmptyDetail(normalizedMode);const emptyNode=nextPanel.querySelector("[data-empty-detail]");if(emptyNode){emptyNode.setAttribute("data-empty-mode",normalizedMode);}
nextPanel.dataset.loadedId="";nextPanel.dataset.matricula="";syncPdfModalDocs(nextPanel);if(normalizedMode==="awaiting"){initializeAdminSummary(nextPanel);}
activePanel=nextPanel;inactivePanel=currentActive;currentActive.classList.add(direction==="forward"?"slide-out-left":"slide-out-right");nextPanel.classList.add(direction==="forward"?"slide-in-right":"slide-in-left");nextPanel.classList.add("active");if(normalizedMode==="awaiting"){requestAnimationFrame(()=>{const summaryRoot=nextPanel.querySelector("[data-admin-summary]");scheduleDonutReplay(summaryRoot||nextPanel);});}
currentActive.classList.remove("active");currentActive.addEventListener("animationend",()=>{currentActive.classList.remove("slide-out-left","slide-out-right");nextPanel.classList.remove("slide-in-left","slide-in-right");},{once:true});lastEmptyPanel=nextPanel;currentEmptyMode=normalizedMode;}
function clearSelectionAndDetail(options={}){const preserveQuery=Boolean(options.preserveQuery);const desiredMode=typeof options.emptyMode==="string"?options.emptyMode:"awaiting";const rows=Array.from(document.querySelectorAll(".guarantees-table__row"));rows.forEach((r)=>r.classList.remove("selected"));prevSelectedRow=null;prevIdx=null;if(!preserveQuery){history.replaceState(null,"",window.location.pathname);pendingMatSelection=false;initialMatQuery="";}
setEmptyDetailPanel("forward",desiredMode);}
function setResultMessage(msg=""){resultMessage.innerHTML=msg;resultMessage.style.display=msg?"block":"none";}
async function loadPage(page=1,options={}){if(isLoading||!hasMore)return;isLoading=true;spinner.style.display="";const search=typeof options.search==="string"?options.search:searchQuery;const estado=typeof options.estado==="string"?options.estado:selectedEstado;const plan=typeof options.plan!=="undefined"?options.plan:selectedPlan;const canal=typeof options.canal==="string"?options.canal:selectedCanal;const concesionario=typeof options.concesionario!=="undefined"?options.concesionario:selectedConcesionario;const vendorType=typeof options.vendorType==="string"?options.vendorType:selectedVendorType;const paymentMethod=typeof options.paymentMethod==="string"?options.paymentMethod:selectedPaymentMethod;const commercial=typeof options.commercial==="string"?options.commercial:options.commercial!=null?String(options.commercial):selectedCommercial;const orderBy=typeof options.orderBy==="string"&&options.orderBy?options.orderBy:selectedOrderBy;const orderDirection=typeof options.orderDirection==="string"&&options.orderDirection?options.orderDirection:selectedOrderDirection;const cacheKey=buildListCacheKey(search,estado,plan,canal,concesionario,vendorType,paymentMethod,orderBy,orderDirection,commercial);try{if(currentListAbort)currentListAbort.abort();currentListAbort=new AbortController();const params=new URLSearchParams({page,per_page:perPage,});if(search&&search.length>0)
params.append("search",search);if(estado)params.append("estado",estado);if(plan)params.append("plan",plan);if(canal)params.append("canal",canal);if(concesionario)
params.append("concesionario",concesionario);if(vendorType)
params.append("vendor_type",vendorType);if(paymentMethod)
params.append("payment_method",paymentMethod);if(commercial)
params.append("commercial",commercial);if(orderBy)
params.append("order_by",orderBy);if(orderDirection)
params.append("order",orderDirection);let url=`${restRoot}go/v1/guarantees?${params.toString()}`;const res=await fetch(url,{headers:{"X-WP-Nonce":restNonce},signal:currentListAbort.signal,});if(!res.ok)throw`HTTP ${res.status}`;totalPosts=+res.headers.get("X-WP-Total")||0;totalPages=+res.headers.get("X-WP-TotalPages")||1;const{data}=await res.json();const esNuevaBusqueda=page===1;if(esNuevaBusqueda){listCache.set(cacheKey,{data:data.slice(),totalPages,totalPosts,});}else if(listCache.has(cacheKey)){const cache=listCache.get(cacheKey);cache.data.push(...data);cache.totalPages=totalPages;cache.totalPosts=totalPosts;}
if(esNuevaBusqueda){setResultMessage("");tbody.innerHTML="";clearSelectionAndDetail({preserveQuery:pendingMatSelection});if(data.length>0){listContainer.scrollTop=0;if(search.length>=3&&data.length>0&&data.length<=20){lastValidQuery=search;lastValidResults=data.slice();}}}
currentPage=page;hasMore=currentPage<totalPages;if(data.length>0){for(const item of data){if(item.detail){detailCache.set(String(item.id),normalizeDetailData(item.detail));}
if(loadedIds.has(item.id))continue;tbody.appendChild(renderRow(item));loadedIds.add(item.id);}
setResultMessage("");if(data.length===1&&search&&search.length>0){const row=tbody.querySelector(".guarantees-table__row");if(row&&!row.classList.contains("selected")){row.classList.add("selected");const id=row.dataset.id;const rowData=buildRowData(row);const currentActive=activePanel;const nextPanel=activePanel===panel1?panel2:panel1;if(detailCache.has(id)){const dataDetalle=detailCache.get(id);nextPanel.innerHTML=renderFullDetail(dataDetalle,rowData,[]);setupTransferCountdown(nextPanel);nextPanel.dataset.matricula=dataDetalle.matricula||rowData.matricula||"";syncPdfModalDocs(nextPanel);}else{nextPanel.classList.add("loading");nextPanel.innerHTML='<div class="spinner" aria-hidden="true"></div>';fetchDetail(id).then((dataDetalle)=>{if(nextPanel.dataset.loadedId===String(id)){nextPanel.innerHTML=renderFullDetail(dataDetalle,rowData,[]);setupTransferCountdown(nextPanel);nextPanel.dataset.matricula=dataDetalle.matricula||rowData.matricula||"";syncPdfModalDocs(nextPanel);}}).catch(()=>{}).finally(()=>{nextPanel.classList.remove("loading");});}
nextPanel.dataset.loadedId=id;activePanel=nextPanel;inactivePanel=currentActive;currentActive.classList.remove("active");nextPanel.classList.add("active");}}}else{if(esNuevaBusqueda&&search.length>=3&&lastValidResults.length>0&&lastValidQuery.length>=3&&lastValidResults.length<=20&&search.length>0){for(const item of lastValidResults){tbody.appendChild(renderRow(item));}
setResultMessage(`No se han encontrado garantías para <strong>"${search}"</strong>. Mostrando resultados de <strong>"${lastValidQuery}"</strong>.`);}else if(search&&page===1){setResultMessage(`No se han encontrado garantías para <strong>"${search}"</strong>.`);}else if(esNuevaBusqueda&&!hasActiveFilters()){const emptyRow=renderEmptyRow();if(emptyRow){tbody.appendChild(emptyRow);}
setResultMessage("");setEmptyDetailPanel("forward","no-results");}else if(esNuevaBusqueda){const emptyRow=renderEmptyRow();if(emptyRow){tbody.appendChild(emptyRow);}
setResultMessage("No se han encontrado garantías con los filtros seleccionados.");}}}catch(err){console.error("❌ Error en loadPage:",err);}finally{isLoading=false;spinner.style.display=hasMore?"":"none";}}
async function preloadByPlate(plate){const trimmedPlate=typeof plate==="string"?plate.trim():"";if(!trimmedPlate){pendingMatSelection=false;initialMatQuery="";return;}
try{const params=new URLSearchParams({search:trimmedPlate,per_page:1,});const res=await fetch(`${restRoot}go/v1/guarantees?${params.toString()}`,{headers:{"X-WP-Nonce":restNonce}});if(!res.ok)throw res.status;const{data}=await res.json();if(data.length===0){pendingMatSelection=false;initialMatQuery="";return;}
const item=data[0];const id=item.id;if(item.detail){detailCache.set(String(id),normalizeDetailData(item.detail));}
let row=tbody.querySelector(`.guarantees-table__row[data-id="${id}"]`);if(!row){row=renderRow(item);tbody.insertBefore(row,tbody.firstChild);loadedIds.add(id);}
if(prevSelectedRow)prevSelectedRow.classList.remove("selected");row.classList.add("selected");const allRows=Array.from(document.querySelectorAll(".guarantees-table__row"));prevIdx=allRows.indexOf(row);const selectedPlate=row.dataset.matricula||trimmedPlate;if(selectedPlate){history.replaceState(null,"",`?matricula=${encodeURIComponent(selectedPlate)}`);}
pendingMatSelection=false;initialMatQuery="";prevSelectedRow=row;const rowData=buildRowData(row);const currentActive=activePanel;const nextPanel=activePanel===panel1?panel2:panel1;nextPanel.classList.add("loading");nextPanel.innerHTML='<div class="spinner" aria-hidden="true"></div>';fetchDetail(id).then((detailData)=>{if(nextPanel.dataset.loadedId===String(id)){nextPanel.innerHTML=renderFullDetail(detailData,rowData,[]);setupTransferCountdown(nextPanel);nextPanel.dataset.matricula=detailData.matricula||rowData.matricula||"";syncPdfModalDocs(nextPanel);}}).catch(()=>{}).finally(()=>{nextPanel.classList.remove("loading");});nextPanel.dataset.loadedId=id;activePanel=nextPanel;inactivePanel=currentActive;currentActive.classList.remove("active");nextPanel.classList.add("active");}catch(e){console.error("❌ Error preloadByPlate:",e);pendingMatSelection=false;initialMatQuery="";}}
function getDurationMeses(desde,hasta){const d1=new Date(desde);const d2=new Date(hasta);if(isNaN(d1)||isNaN(d2))return"-";let months=(d2.getFullYear()-d1.getFullYear())*12+
(d2.getMonth()-d1.getMonth());if(d2.getDate()>=d1.getDate())months++;return months>0?months:"-";}
function getRestantesMeses(hasta){const hoy=new Date();const fin=new Date(hasta);if(isNaN(fin))return"-";let months=(fin.getFullYear()-hoy.getFullYear())*12+
(fin.getMonth()-hoy.getMonth());if(fin.getDate()>=hoy.getDate())months++;return months>0?months:"-";}
function buildRowData(row){return{marca_modelo:row.dataset.marca_modelo??"-",matricula:row.dataset.matricula??"-",plan:row.dataset.plan??"-",desde:row.dataset.desde??"-",desde_raw:row.dataset.desdeRaw??row.dataset.desde??"",desde_fmt:row.dataset.desdeFmt??(row.dataset.desde?formatDate(row.dataset.desde).display:"-"),hasta:row.dataset.hasta??"-",hasta_raw:row.dataset.hastaRaw??row.dataset.hasta??"",hasta_fmt:row.dataset.hastaFmt??(row.dataset.hasta?formatDate(row.dataset.hasta).display:"-"),estado:row.dataset.estado??"Desconocido",estadoclase:row.dataset.estadoclase??"pendiente-pago",concesionario:row.dataset.vendedor_name??"-",canal_venta:row.dataset.vendedor_type??"-",precio:row.dataset.precio??"-",metodo_pago:row.dataset.metodoPago??"",cobro_realizado:row.dataset.cobroRealizado==="1",iban_vendedor:row.dataset.ibanVendedor??"",transfer_iban:row.dataset.transferIban??"",tipo:"-",kilometros:"-",primera_matriculacion:"-",bastidor:"-",precio_venta:"-",combustible:"-",cambio:"-",potencia:"-",cilindrada:"-",telefono_vendedor:"-",email_vendedor:"-",avatar_vendedor:"",vendedor_url:"#",certificate_url:"#",condicionado_url:"#",cobertura_url:"#",nombre_comprador:row.dataset.compradorNombre??"-",dni_comprador:"-",telefono_comprador:(row.dataset.compradorTelefono??"-")||"-",email_comprador:"-",direccion_comprador:"-",localidad_comprador:"-",provincia_comprador:"-",codigo_postal_comprador:"-",};}
function renderFastActions(telefono,email,skeletons=[],telField="telefono_vendedor",emailField="email_vendedor"){const tel=skeletons.includes(telField)?"":telefono??"";const mail=skeletons.includes(emailField)?"":email??"";const telHtml=tel?`<li class="fast-actions__item"><a href="tel:${tel}" class="fast-actions__link"><span class="fast-actions__icon">${phoneIcon}</span><span class="fast-actions__label">${tel}</span></a></li>`:"";const mailHtml=mail?`<li class="fast-actions__item"><a href="mailto:${mail}" class="fast-actions__link"><span class="fast-actions__icon">${emailIcon}</span><span class="fast-actions__label">${mail}</span></a></li>`:"";const content=`${telHtml}${mailHtml}`;return content?`<ul class="fast-actions">${content}</ul>`:"";}
function renderFullDetail(data,rowData,skeletons=[]){const getFieldText=(val)=>val&&typeof val==="object"&&"label"in val?val.label:val;const escapeAttr=(value)=>String(value).replace(/&/g,"&amp;").replace(/"/g,"&quot;").replace(/</g,"&lt;").replace(/>/g,"&gt;");const skeleton=(field,fallback="-")=>skeletons.includes(field)?`<span class="skeleton skeleton--${field}"></span>`:getFieldText(data[field])??getFieldText(rowData[field])??fallback;const mesesTotales=getDurationMeses(data.desde,data.hasta);const mesesRestantes=getRestantesMeses(data.hasta);const estadoValue=(data.estado&&data.estado.value)||rowData.estadoclase||"pendiente-pago";const estadoClase=normalizeEstadoClase(estadoValue);const isSinFinalizar=estadoClase==="sin-finalizar";const isPendientePago=estadoClase==="pendiente-pago";const isValidacionPendiente=estadoClase==="validacion-pendiente";const canShowReportBtn=estadoClase==="activada";const badgeClase=`guarantee-detail__badge guarantee-detail__badge--${estadoClase}`;const metodoPago=(data.metodo_pago??rowData.metodo_pago??"").toString().toLowerCase().trim();const cobroRealizado=[data.cobro_realizado,rowData.cobro_realizado,].some((v)=>v===true||v===1||v==="1");const transferIban=[data.transfer_iban,rowData.transfer_iban].map((val)=>(typeof val==="string"?val.trim():"")).find((val)=>val)||"";const planName=skeleton("plan","-");const planParts=[];if(planName&&planName!=="-"){planParts.push(planName);}
if(mesesTotales!=="-"){planParts.push(`${mesesTotales} meses`);}
const planPrimaryLabel=planParts.length>0?planParts.join(" "):planName||"-";const planPriceRaw=skeleton("precio","");const planPrice=planPriceRaw&&planPriceRaw!=="-"?`${planPriceRaw} €`:"";const planTitleHtml=`<h3 class="guarantee-detail__plan-title">`+`<span class="guarantee-detail__plan-name">${planPrimaryLabel}</span>`+`${planPrice ? `<span class=\"guarantee-detail__plan-price\">${planPrice}</span>` : ""}` +
        `</h3>`;
    const vendorChannelSummaryRaw = skeleton("canal_venta_summary", "");
    const vendorChannelSummarySource =
        vendorChannelSummaryRaw !== ""
            ? vendorChannelSummaryRaw
            : skeleton("canal_venta", "-");
    const vendorChannelSummary =
        vendorChannelSummarySource && !vendorChannelSummarySource.includes("skeleton")
            ? extractVendorType(vendorChannelSummarySource) || vendorChannelSummarySource
            : vendorChannelSummarySource;
    const vendorCompanyName = skeleton("concesionario", "-");
    const vendorContactRaw = skeleton("concesionario_personal", "");
    const vendorContactName =
        vendorContactRaw !== "" ? vendorContactRaw : vendorCompanyName;
    const vendorAvatarUrl =
        data.avatar_vendedor ?? rowData.avatar_vendedor ?? "";
    const vendorAvatarWrapper = vendorAvatarUrl
        ? `<div class="vendor-card__avatar-wrapper"><img src="${escapeAttr(vendorAvatarUrl)}" alt="" class="vendor-card__avatar"></div>`
        : `<div class="vendor-card__avatar-wrapper vendor-card__avatar-wrapper--icon"><span class="vendor-card__avatar vendor-card__avatar--icon">${userIcon}</span></div>`;
    const vendorActionsHtml = renderFastActions(
        data.telefono_vendedor ?? rowData.telefono_vendedor,
        data.email_vendedor ?? rowData.email_vendedor,
        skeletons
    );
    let vendorDetailsHrefRaw = data.vendor_profile_url ?? data.vendedor_url ?? rowData.vendedor_url ?? "";
    if ((!vendorDetailsHrefRaw || vendorDetailsHrefRaw === "#") && data.vendor_slug) {
        const clientsBase = (goConfig.pages && goConfig.pages.clientes) || "";
        if (clientsBase) {
            const base = clientsBase.replace(/\/+$/, "");
            vendorDetailsHrefRaw = `${base}/${data.vendor_slug}/`;
        }
    }
    const vendorDetailsHref = escapeAttr(vendorDetailsHrefRaw && vendorDetailsHrefRaw !== "#" ? vendorDetailsHrefRaw : "#");
    const fuelRaw = (
        data.combustible ?? rowData.combustible ?? ""
    )
        .toString()
        .toLowerCase();
    const isElectric = fuelRaw
        .normalize("NFD")
        .replace(/[\u0300-\u036f]/g, "") === "electrico";
    const potenciaUnidad = isElectric ? "kW" : "CV";

    const isFilled = (val) => {
        if (val === undefined || val === null) return false;
        const str = String(val).trim();
        return str !== "" && str !== "-" && str !== "#";
    };
    const docsSource = Array.isArray(data.documents)
        ? data.documents
        : Array.isArray(rowData.documents)
        ? rowData.documents
        : [];
    const docsData = docsSource
        .map((doc) => {
            const key = typeof doc.key === "string" && doc.key !== "" ? doc.key : doc.row ? `extra-${doc.row}` : "";
            const base = key && docsConfigMap.has(key) ? docsConfigMap.get(key) : null;
            const listLabel =
                (typeof doc.listLabel === "string" && doc.listLabel.trim() !== "" ? doc.listLabel : null) ||
                (typeof doc.title === "string" && doc.title.trim() !== "" ? doc.title : null) ||
                (base && base.listLabel) ||
                (doc.kind === "transfer_receipt" ? "Justificante de transferencia" : "Documento");
            const downloadLabel =
                (typeof doc.downloadLabel === "string" && doc.downloadLabel.trim() !== "" ? doc.downloadLabel : null) ||
                (base && base.successLabel) ||
                "Descargar documento";
            let iconKey = "";
            if (base && base.icon) {
                iconKey = base.icon.toLowerCase();
            }
            if (typeof doc.icon === "string" && doc.icon.trim() !== "") {
                const candidate = doc.icon.trim().toLowerCase();
                if (!iconKey || candidate !== "download") {
                    iconKey = candidate;
                }
            }
            if (!iconKey) {
                iconKey = doc.kind === "transfer_receipt" ? "payment" : "pdf";
            }
            const rawUrl = doc.url || doc.attachment_url || doc.direct_url || "";
            const url = normalizeDocUrl(rawUrl);
            return {
                key: key || `doc-${Math.random().toString(16).slice(2)}`,
                url,
                listLabel,
                downloadLabel,
                iconKey,
                mime: typeof doc.mime === "string" ? doc.mime : "",
                extension: typeof doc.extension === "string" ? doc.extension : "",
                filename: typeof doc.filename === "string" ? doc.filename : "",
                kind: typeof doc.kind === "string" ? doc.kind : "general",
                row: doc.row || "",
            };
        })
        .filter((doc) => doc.url);
    docsData.forEach((doc) => scheduleDocumentPreload(doc.url));
    const buyerFields = [
        "nombre_comprador",
        "dni_comprador",
        "telefono_comprador",
        "email_comprador",
        "direccion_comprador",
        "localidad_comprador",
        "provincia_comprador",
        "codigo_postal_comprador",
    ];
    const hasGuaranteeInfo =
        isFilled(data.plan) && isFilled(data.desde_fmt) && isFilled(data.hasta_fmt);
    const hasDocs = docsData.length > 0;
    const docsButtonsHtml = docsData
        .map(
            (doc, idx) => {
                const rawClassKey = typeof doc.key === "string" ? doc.key : String(doc.key || "");
                const safeClassName = rawClassKey
                    ? rawClassKey.toLowerCase().replace(/[^a-z0-9_-]/g, "")
                    : "";
                const buttonClass = safeClassName !== "" ? `detail__docs-btn--${safeClassName}` : "";
                const iconMarkup = doc.iconKey === "payment"
                    ? paymentIcon
                    : doc.iconKey === "download" && downloadIcon !== ""
                    ? downloadIcon
                    : pdfIcon;
                const attrs = [
                    `data-doc-key="${escapeAttr(doc.key)}"`,
                    `data-doc-index="${idx}"`,
                    `data-doc-url="${escapeAttr(doc.url)}"`,
                ];
                if (doc.mime) attrs.push(`data-doc-mime="${escapeAttr(doc.mime)}"`);
                if (doc.extension) attrs.push(`data-doc-extension="${escapeAttr(doc.extension)}"`);
                if (doc.filename) attrs.push(`data-doc-filename="${escapeAttr(doc.filename)}"`);
                if (doc.downloadLabel) attrs.push(`data-doc-download="${escapeAttr(doc.downloadLabel)}"`);
                if (doc.kind) attrs.push(`data-doc-kind="${escapeAttr(doc.kind)}"`);
                if (doc.row) attrs.push(`data-doc-row="${escapeAttr(String(doc.row))}"`);
                return (
                    `<li class="detail__docs-item">` +
                    `<button type="button"class="detail__docs-btn${buttonClass}"${attrs.join(" ")}aria-label="Ver documento ${escapeAttr(doc.listLabel)}">` +
                    `<span class="detail__docs-icon detail__docs-icon--${doc.iconKey}">${iconMarkup}</span>` +
                    `<span class="detail__docs-label">${doc.listLabel}</span>` +
                    `</button>` +
                    `</li>`
                );
            }
        )
        .join("");

    const addDocButtonHtml = isAdmin
        ? `<li class="detail__docs-item detail__docs-item--add">` +
          `<button type="button"class="detail__docs-btn detail__docs-btn--add detail__docs-add"data-doc-key="${ADD_DOC_KEY}"data-doc-action="add"aria-label="Añadir documento">` +
          `<span class="detail__docs-icon detail__docs-icon--add">${plusIcon||"+"}</span>` +
          `<span class="detail__docs-label">Añadir documento</span>` +
          `</button>` +
          `</li>`
        : "";

    let docsListHtml = "";
    if (hasDocs || isAdmin) {
        docsListHtml = `<ul class="detail__docs-list">${docsButtonsHtml}${addDocButtonHtml}</ul>`;
        if (!hasDocs) {
            docsListHtml = `<p class="detail__alert-section">Documentación no disponible</p>${docsListHtml}`;
        }
    } else {
        docsListHtml = `<p class="detail__alert-section">Documentación no disponible</p>`;
    }
    const hasBuyerInfo = buyerFields.every((field) => isFilled(data[field]));
    const showChannelSection = isAdmin;
    const showActions = isAdmin;

    const sinFinalButtons = [];
    if (showActions || isProfesional) {
        sinFinalButtons.push(
            `<button type="button"aria-label="Continuar con la garantía"class="guarantee-detail__btn guarantee-detail__btn--continue">` +
                `<span class="guarantee-detail__btn-icon">${continueIcon}</span>` +
                `<span class="guarantee-detail__btn-text">Continuar con la garantía</span>` +
            `</button>`
        );
        if (showActions) {
            sinFinalButtons.push(
                `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--fav"aria-label="Guardar en favoritos">` +
                    `<span class="guarantee-detail__btn-icon">${heartIcon}</span>` +
                `</button>`
            );
            sinFinalButtons.push(
                `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--share"aria-label="Compartir">` +
                    `<span class="guarantee-detail__btn-icon">${shareIcon}</span>` +
                `</button>`
            );
        }
    }
    const sinFinalActionsHtml = sinFinalButtons.length
        ? `<div class="guarantee-detail__btn-container">${sinFinalButtons.join("")}</div>`
        : "";

    if (isSinFinalizar) {
        return `<div class="guarantee-detail__inner"><div class="guarantee-detail__header"><h2>Garantía ${skeleton("matricula")}</h2>${hasGuaranteeInfo?`${planTitleHtml}
                        <div><p>${skeleton("desde_fmt")} — ${skeleton("hasta_fmt")}
                                <span class="guarantee-detail__plan-duration">(${mesesRestantes !== "-" ? mesesRestantes + " meses restantes" : "-"})</span></p></div>`:""}<div><p class="detail__alert-section">Completa los datos pendientes para tramitar la garantía</p></div><div class="${badgeClase}">${skeleton("estado","Desconocido")}</div></div>${showChannelSection?`<section class="detail__section detail__section--channel">
                                <h3 class="detail__section-title">
                                        <span>Canal de venta</span>
                                </h3>
                                <div class="vendor-card">
                                        <div class="vendor-card__header">
                                                <div class="vendor-card__primary">
                                                        ${vendorAvatarWrapper}
                                                        <div class="vendor-card__info">
                                                                <p class="vendor-card__name">${vendorCompanyName}</p>
                                                                <p class="vendor-card__contact">${vendorContactName}</p>
                                                        </div>
                                                </div>
                                                ${vendorChannelSummary ? `<span class="vendor-card__badge">${vendorChannelSummary}</span>` : ""}
                                        </div>
                                        ${vendorActionsHtml
                                            ? `<div class="vendor-card__actions">${vendorActionsHtml}</div>`
                                            : ``}
                                        <div class="vendor-card__footer">
                                                <a href="${vendorDetailsHref}" class="vendor-card__cta vendor-card__cta--details">
                                                        <span class="vendor-card__cta-icon" aria-hidden="true">${personIcon}</span>
                                                        <span class="vendor-card__cta-label">Ver ficha del cliente</span>
                                                </a>
                                                <button type="button" class="vendor-card__cta vendor-card__cta--contact">
                                                        <span class="vendor-card__cta-icon" aria-hidden="true">${personAddIcon}</span>
                                                        <span class="vendor-card__cta-label">Añadir contacto</span>
                                                </button>
                                        </div>
                                </div>
                        </section>`:""}<section class="detail__section"><h3>Datos del vehículo</h3><ul><li><strong>Marca/Modelo:</strong>${skeleton("marca_modelo")}</li><li><strong>Tipo:</strong>${skeleton("tipo","-")}</li><li><strong>Kilómetros:</strong>${skeleton("kilometros","-")}km</li><li><strong>1ª Matriculación:</strong>${skeleton("primera_matriculacion","-")}</li><li><strong>Matrícula:</strong>${skeleton("matricula")}</li><li><strong>Nº Bastidor:</strong>${skeleton("bastidor","-")}</li><li><strong>Precio venta:</strong>${skeleton("precio_venta","-")}€</li></ul></section><section class="detail__section"><h3>Detalles técnicos</h3><ul><li><strong>Combustible:</strong>${skeleton("combustible","-")}</li><li><strong>Cambio:</strong>${skeleton("cambio","-")}</li><li><strong>Potencia:</strong>${skeleton("potencia","-")}${potenciaUnidad}</li><li><strong>Cilindrada:</strong>${skeleton("cilindrada","-")}CC</li></ul></section><section class="detail__section detail__section--docs"><h3 class="detail__section-title">Documentación</h3>${docsListHtml}</section><section class="detail__section detail__section--datos_cliente"><h3>Datos del cliente</h3>${hasBuyerInfo?`<ul>
                                <li><strong>Nombre:</strong> ${skeleton("nombre_comprador", "-")}</li>
                                <li><strong>DNI/NIE:</strong> ${skeleton("dni_comprador", "-")}</li>
                                <li><strong>Teléfono:</strong> ${skeleton("telefono_comprador", "-")}</li>
                                <li><strong>Email:</strong> ${skeleton("email_comprador", "-")}</li>
                                <li><strong>Dirección:</strong> ${skeleton("direccion_comprador", "-")}</li>
                                <li><strong>Localidad:</strong> ${skeleton("localidad_comprador", "-")}</li>
                                <li><strong>Provincia:</strong> ${skeleton("provincia_comprador", "-")}</li>
                        
                                <li><strong>Código Postal:</strong> ${skeleton("codigo_postal_comprador", "-")}</li>
                        </ul>
                        ${renderFastActions(
                                data.telefono_comprador ?? rowData.telefono_comprador,
                                data.email_comprador ?? rowData.email_comprador,
                                skeletons,
                                "telefono_comprador",
                                "email_comprador"
                        )}`:`<p class="detail__alert-section">Faltan datos del cliente</p>`}</section>${sinFinalActionsHtml}</div>`;
    }

    const adminPendingDomiciliacion =
        isAdmin && metodoPago.startsWith("domiciliacion") && !cobroRealizado;
    const deadlineInfo = getTransferDeadlineInfo(data, rowData);
    const transferDeadlineMs = deadlineInfo.deadlineMs;
    const hasTransferDeadline = Number.isFinite(transferDeadlineMs);
    const transferDeadlineExpired = hasTransferDeadline && Boolean(deadlineInfo.expired);
    const shouldShowConfirmBtn =
        showActions && (isPendientePago || isValidacionPendiente || adminPendingDomiciliacion);
    const confirmLabel =
        adminPendingDomiciliacion || metodoPago.startsWith("domiciliacion")
            ? "Confirmar domiciliación"
            : "Confirmar pago";
    const professionalActionButtons = [];
    if (
        isProfesional &&
        isPendientePago &&
        metodoPago === "transferencia" &&
        (!hasTransferDeadline || !transferDeadlineExpired)
    ) {
        professionalActionButtons.push(
            `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--transfer-report"aria-label="Ya he realizado la transferencia">` +
                `<span class="guarantee-detail__btn-icon">${paymentIcon}</span>` +
                `<span class="guarantee-detail__btn-text">Ya he realizado la transferencia</span>` +
            `</button>`
        );
    }

    const adminActionButtons = [];
    if (showActions) {
        if (shouldShowConfirmBtn) {
            adminActionButtons.push(
                `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--confirm"aria-label="${confirmLabel}">` +
                    `<span class="guarantee-detail__btn-icon">${paymentIcon}</span>` +
                    `<span class="guarantee-detail__btn-text">${confirmLabel}</span>` +
                `</button>`
            );
        }
        if (canShowReportBtn) {
            adminActionButtons.push(
                `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--report"aria-label="Abrir expediente para esta garantía">` +
                    `<span class="guarantee-detail__btn-icon">${warningIcon}</span>` +
                    `<span class="guarantee-detail__btn-text">Abrir expediente</span>` +
                `</button>`
            );
        }
        adminActionButtons.push(
            `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--fav"aria-label="Guardar en favoritos">` +
                `<span class="guarantee-detail__btn-icon">${heartIcon}</span>` +
            `</button>`
        );
        adminActionButtons.push(
            `<button type="button"class="guarantee-detail__btn guarantee-detail__btn--share"aria-label="Compartir">` +
                `<span class="guarantee-detail__btn-icon">${shareIcon}</span>` +
            `</button>`
        );
    }
    const combinedActionButtons = [
        ...professionalActionButtons,
        ...adminActionButtons,
    ];
    const actionsHtml = combinedActionButtons.length
        ? `<div class="guarantee-detail__btn-container">${combinedActionButtons.join("")}</div>`
        : "";

    const { vendorDisplayHtml, adminEntityHtml } = (() => {
        const vendorCompanyData =
            data && typeof data.vendor_company === "object" && data.vendor_company !== null
                ? data.vendor_company
                : null;
        const normalizeName = (value) => (typeof value === "string" ? value.trim() : "");
        const vendorTradeName = normalizeName(vendorCompanyData?.trade_name);
        const vendorCompanyLabel = normalizeName(vendorCompanyData?.name);
        const vendorFallbackName = vendorCompanyName !== "-" ? vendorCompanyName : "";
        const vendorLegalName = normalizeName(vendorCompanyData?.legal_name);
        const vendorDisplayName =
            vendorTradeName || vendorCompanyLabel || normalizeName(vendorFallbackName) || vendorLegalName;
        const vendorDisplayHtml = vendorDisplayName ? `<strong>${escapeHtml(vendorDisplayName)}</strong>` : "";
        return {
            vendorDisplayHtml,
            adminEntityHtml: vendorDisplayHtml || "El cliente",
        };
    })();
    const deadlineLabelHtml = deadlineInfo.label ? escapeHtml(deadlineInfo.label) : "";
    const professionalExpiredHtml = deadlineLabelHtml
        ? `El plazo para realizar la transferencia venció el ${deadlineLabelHtml}.Ponte en contacto con tu comercial asignado.`
        : escapeHtml(
              "El plazo para realizar la transferencia venció. Ponte en contacto con tu comercial asignado."
          );
    const adminExpiredBaseHtml = deadlineLabelHtml
        ? `El plazo para realizar la transferencia venció el ${deadlineLabelHtml}.`
        : escapeHtml("El plazo para realizar la transferencia ha vencido.");
    const adminExpiredHtml = vendorDisplayHtml
        ? `${adminExpiredBaseHtml}Ponte en contacto con ${vendorDisplayHtml}.`
        : `${adminExpiredBaseHtml}Ponte en contacto con el cliente.`;
    const professionalActiveHtml = hasTransferDeadline && deadlineLabelHtml
        ? `Recuerda realizar la transferencia antes del ${deadlineLabelHtml}para activar la garantía.`
        : escapeHtml("Recuerda realizar la transferencia para activar tu garantía.");
    const adminActiveHtml = hasTransferDeadline && deadlineLabelHtml
        ? `${adminEntityHtml}tiene hasta el ${deadlineLabelHtml}para realizar la transferencia.`
        : `${adminEntityHtml}debe realizar la transferencia para activar la garantía.`;
    const buildNoteHtml = (activeHtml, expiredHtml) => {
        const classes = ["detail__payment-note"];
        let content = activeHtml;
        if (hasTransferDeadline && deadlineInfo.expired) {
            classes.push("detail__payment-note--expired");
            content = expiredHtml;
        }
        return `<p class="${classes.join("")}">${content}</p>`;
    };
    const professionalNoteHtml = buildNoteHtml(professionalActiveHtml, professionalExpiredHtml);
    const adminNoteHtml = buildNoteHtml(adminActiveHtml, adminExpiredHtml);
    const professionalValidationHtml = escapeHtml(
        "Has confirmado que has realizado la transferencia. El equipo de 360VO revisará la información y activará tu garantía en un plazo máximo de 72 horas. Recibirás un correo cuando esté activa."
    );
    const validationBaseHtml = vendorDisplayHtml
        ? `${vendorDisplayHtml}ha indicado que ha realizado la transferencia.`
        : escapeHtml("El cliente ha indicado que ha realizado la transferencia.");
    const adminValidationHtml = `${validationBaseHtml}Revisa la operación y activa la garantía cuando proceda.`;
    const staffValidationHtml = validationBaseHtml;

    const paymentHtml = (() => {
        if (isAdmin && metodoPago.startsWith("domiciliacion") && !cobroRealizado) {
            const concepto = `Garantía ${skeleton("matricula")}`;
            const cantidad = `${skeleton("precio","0")}€`;
            const iban =
                data.iban_vendedor ||
                rowData.iban_vendedor ||
                "ES00 0000 0000 0000 0000 0000";
            return `<section class="detail__section detail__section--payment"><p class="detail__payment-note detail__payment-note--domiciliacion">Cobro pendiente por domiciliación bancaria.</p><table class="detail__transfer-table"><tbody><tr data-copy-row><th>Concepto</th><td data-copy-cell data-tooltip="Copiar concepto"><span data-concepto>${concepto}</span><button type="button"class="detail__copy-btn"data-copy="[data-concepto]"data-label="Copiar concepto"data-done="Concepto copiado"data-toast="Concepto copiado al portapapeles."aria-label="Copiar concepto">${copyIcon}</button></td></tr><tr data-copy-row><th>Cantidad</th><td data-copy-cell data-tooltip="Copiar cantidad"><span data-amount>${cantidad}</span><button type="button"class="detail__copy-btn"data-copy="[data-amount]"data-label="Copiar cantidad"data-done="Cantidad copiada"data-toast="Cantidad copiada al portapapeles."aria-label="Copiar cantidad">${copyIcon}</button></td></tr><tr data-copy-row><th>IBAN</th><td data-copy-cell data-tooltip="Copiar IBAN"><span data-iban>${iban}</span><button type="button"class="detail__copy-btn"data-copy="[data-iban]"data-label="Copiar IBAN"data-done="IBAN copiado"data-toast="IBAN copiado al portapapeles."aria-label="Copiar IBAN">${copyIcon}</button></td></tr></tbody></table><div class="detail__copy-toast"aria-hidden="true"></div></section>`;
        }
        if ((isPendientePago || isValidacionPendiente) && metodoPago === "transferencia") {
            const concepto = `Garantía ${skeleton("matricula")}`;
            const cantidad = `${skeleton("precio","0")}€`;
            const ibanRow = transferIban
                ? `<tr data-copy-row><th>IBAN</th><td data-copy-cell data-tooltip="Copiar IBAN"><span data-iban>${transferIban}</span><button type="button"class="detail__copy-btn"data-copy="[data-iban]"data-label="Copiar IBAN"data-done="IBAN copiado"data-toast="IBAN copiado al portapapeles."aria-label="Copiar IBAN">${copyIcon}</button></td></tr>`
                : "";
            const noteHtml = isValidacionPendiente
                ? `<p class="detail__payment-note detail__payment-note--validation">${isAdmin?isCoreAdmin?adminValidationHtml:staffValidationHtml:professionalValidationHtml}</p>`
                : isAdmin
                ? adminNoteHtml
                : professionalNoteHtml;
            const tableHtml = `<table class="detail__transfer-table"><tbody><tr data-copy-row><th>Concepto</th><td data-copy-cell data-tooltip="Copiar concepto"><span data-concepto>${concepto}</span><button type="button"class="detail__copy-btn"data-copy="[data-concepto]"data-label="Copiar concepto"data-done="Concepto copiado"data-toast="Concepto copiado al portapapeles."aria-label="Copiar concepto">${copyIcon}</button></td></tr><tr data-copy-row><th>Cantidad</th><td data-copy-cell data-tooltip="Copiar cantidad"><span data-amount>${cantidad}</span><button type="button"class="detail__copy-btn"data-copy="[data-amount]"data-label="Copiar cantidad"data-done="Cantidad copiada"data-toast="Cantidad copiada al portapapeles."aria-label="Copiar cantidad">${copyIcon}</button></td></tr>${ibanRow}</tbody></table>`;
            const toggleLabel = "Ver detalles de la transferencia";
            const closedIcon = arrowDownIcon || "&#9660;";
            const openIcon = arrowUpIcon || "&#9650;";
            const tableMarkup = isAdmin || isValidacionPendiente
                ? `<details class="detail__transfer-toggle"><summary class="detail__transfer-toggle-summary"><span class="detail__transfer-toggle-label">${toggleLabel}</span><span class="detail__transfer-toggle-icon detail__transfer-toggle-icon--closed"aria-hidden="true">${closedIcon}</span><span class="detail__transfer-toggle-icon detail__transfer-toggle-icon--open"aria-hidden="true">${openIcon}</span></summary><div class="detail__transfer-toggle-content">${tableHtml}</div></details>`
                : tableHtml;
            return `<section class="detail__section detail__section--payment">${noteHtml}
${tableMarkup}<div class="detail__copy-toast"aria-hidden="true"></div></section>`;
        }
        if (!isAdmin && isPendientePago) {
            return `<section class="detail__section detail__section--payment"><p class="detail__payment-note">El pago se procesará mediante domiciliación bancaria.</p></section>`;
        }
        return "";
    })();
    return `<div class="guarantee-detail__inner"><div class="guarantee-detail__header"><h2>Garantía ${skeleton("matricula")}</h2>${planTitleHtml}<div><p>${skeleton("desde_fmt")}— ${skeleton("hasta_fmt")}<span class="guarantee-detail__plan-duration">(${mesesRestantes!=="-"?mesesRestantes+" meses restantes":"-"})</span></p></div><div class="${badgeClase}">${skeleton("estado","Desconocido")}</div></div>${paymentHtml}
${showChannelSection?`<section class="detail__section detail__section--channel">
                                <h3 class="detail__section-title">
                                        <span>Canal de venta</span>
                                </h3>
                                <div class="vendor-card">
                                        <div class="vendor-card__header">
                                                <div class="vendor-card__primary">
                                                        ${vendorAvatarWrapper}
                                                        <div class="vendor-card__info">
                                                                <p class="vendor-card__name">${vendorCompanyName}</p>
                                                                <p class="vendor-card__contact">${vendorContactName}</p>
                                                        </div>
                                                </div>
                                                ${vendorChannelSummary ? `<span class="vendor-card__badge">${vendorChannelSummary}</span>` : ""}
                                        </div>
                                        ${vendorActionsHtml
                                            ? `<div class="vendor-card__actions">${vendorActionsHtml}</div>`
                                            : ``}
                                        <div class="vendor-card__footer">
                                                <a href="${vendorDetailsHref}" class="vendor-card__cta vendor-card__cta--details">
                                                        <span class="vendor-card__cta-icon" aria-hidden="true">${personIcon}</span>
                                                        <span class="vendor-card__cta-label">Ver ficha del cliente</span>
                                                </a>
                                                <button type="button" class="vendor-card__cta vendor-card__cta--contact">
                                                        <span class="vendor-card__cta-icon" aria-hidden="true">${personAddIcon}</span>
                                                        <span class="vendor-card__cta-label">Añadir contacto</span>
                                                </button>
                                        </div>
                                </div>
                        </section>`:""}<section class="detail__section"><h3>Datos del vehículo</h3><ul><li><strong>Marca/Modelo:</strong>${skeleton("marca_modelo")}</li><li><strong>Tipo:</strong>${skeleton("tipo","-")}</li><li><strong>Kilómetros:</strong>${skeleton("kilometros","-")}km</li><li><strong>1ª Matriculación:</strong>${skeleton("primera_matriculacion","-")}</li><li><strong>Matrícula:</strong>${skeleton("matricula")}</li><li><strong>Nº Bastidor:</strong>${skeleton("bastidor","-")}</li><li><strong>Precio venta:</strong>${skeleton("precio_venta","-")}€</li></ul></section><section class="detail__section"><h3>Detalles técnicos</h3><ul><li><strong>Combustible:</strong>${skeleton("combustible","-")}</li><li><strong>Cambio:</strong>${skeleton("cambio","-")}</li><li><strong>Potencia:</strong>${skeleton("potencia","-")}${potenciaUnidad}</li><li><strong>Cilindrada:</strong>${skeleton("cilindrada","-")}CC</li></ul></section><section class="detail__section detail__section--docs"><h3 class="detail__section-title">Documentación</h3>${docsListHtml}</section><section class="detail__section detail__section--datos_cliente"><h3>Datos del cliente</h3><ul><li><strong>Nombre:</strong>${skeleton("nombre_comprador","-")}</li><li><strong>DNI/NIE:</strong>${skeleton("dni_comprador","-")}</li><li><strong>Teléfono:</strong>${skeleton("telefono_comprador","-")}</li><li><strong>Email:</strong>${skeleton("email_comprador","-")}</li><li><strong>Dirección:</strong>${skeleton("direccion_comprador","-")}</li><li><strong>Localidad:</strong>${skeleton("localidad_comprador","-")}</li><li><strong>Provincia:</strong>${skeleton("provincia_comprador","-")}</li><li><strong>Código Postal:</strong>${skeleton("codigo_postal_comprador","-")}</li></ul>${renderFastActions(data.telefono_comprador??rowData.telefono_comprador,data.email_comprador??rowData.email_comprador,skeletons,"telefono_comprador","email_comprador")}</section>${actionsHtml}</div>`;
}

function syncPdfModalDocs(panel) {
        const modal = document.querySelector(".pdf-modal");
        if (!modal) return;
        const modalList = modal.querySelector(".pdf-modal__docs-list");
        const panelList = panel.querySelector(".detail__docs-list");
        if (modalList) {
                modalList.innerHTML = "";
        }
        if (modalList && panelList) {
                const panelButtons = Array.from(
                        panelList.querySelectorAll(".detail__docs-btn")
                );
                panelButtons.forEach((btn, idx) => {
                        btn.dataset.docIndex = String(idx);
                });
        panelButtons.forEach((btn, idx) => {
                const item = btn.closest(".detail__docs-item");
                if (!item) return;
                const clone = item.cloneNode(true);
                const cloneBtn = clone.querySelector(
                        ".detail__docs-btn"
                );
                if (cloneBtn) {
                        cloneBtn.dataset.docIndex = String(idx);
                }
                modalList.appendChild(clone);
        });
        updateModalControls(modal);
    } else if (modalList) {
        modalList.innerHTML = "";
        updateModalControls(modal);
    }
    const subtitle = modal.querySelector(".pdf-modal-subttitle");
        if (subtitle) {
                const mat = panel.dataset.matricula || "";
                subtitle.textContent = mat ? `Garantía ${mat}` : "";
        }
}

function updateModalControls(modal) {
        if (!modal) return;
        const buttons = Array.from(
                modal.querySelectorAll(".pdf-modal__docs-list .detail__docs-btn")
        );
        const docButtons = buttons.filter((btn) => btn.dataset.docKey !== ADD_DOC_KEY);
        const download = modal.querySelector(".pdf-modal__download");
        const nav = modal.querySelector(".pdf-modal__nav");
        if (download) {
                download.hidden = docButtons.length === 0;
        }
        if (nav) {
                nav.hidden = docButtons.length <= 1;
        }
}

function initRowSelection() {
                        tbody.addEventListener("click", async function (e) {
				const row = e.target.closest(".guarantees-table__row");
				if (!row) return;
				const rows = Array.from(
					document.querySelectorAll(".guarantees-table__row")
				);
				const idx = rows.indexOf(row);

				// Deselección
				if (row.classList.contains("selected")) {
					rows.forEach((r) => r.classList.remove("selected"));
					prevSelectedRow = null;
					prevIdx = null;
					history.replaceState(null, "", window.location.pathname);
                                   setEmptyDetailPanel(
                                           idx > prevIdx ? "forward" : "back",
                                           "awaiting"
                                   );
					return;
				}

				rows.forEach((r) => r.classList.remove("selected"));
				row.classList.add("selected");
				const id = row.dataset.id;
				history.replaceState(
					null,
					"",
					`?matricula=${encodeURIComponent(row.dataset.matricula)}`
				);
				const forward = prevIdx === null || idx > prevIdx;
				prevIdx = idx;
				const currentActive = activePanel;
				const nextPanel = activePanel === panel1 ? panel2 : panel1;

				const rowData = buildRowData(row);

                                if (detailCache.has(id)) {
                                        const data = detailCache.get(id);
                                        nextPanel.innerHTML = renderFullDetail(data, rowData, []);
                                        setupTransferCountdown(nextPanel);
                                        nextPanel.dataset.matricula = data.matricula || rowData.matricula || "";
                                        nextPanel.dataset.plan = data.plan || rowData.plan || "";
                                        syncPdfModalDocs(nextPanel);
                                } else {
                                        nextPanel.classList.add("loading");
                                        nextPanel.innerHTML = '<div class="spinner" aria-hidden="true"></div>';
                                }
                                nextPanel.dataset.loadedId = id;

                                activePanel = nextPanel;
                                inactivePanel = currentActive;
                                currentActive.classList.add(
                                        forward ? "slide-out-left" : "slide-out-right"
                                );
                                nextPanel.classList.add(forward ? "slide-in-right" : "slide-in-left");
                                nextPanel.classList.add("active");
                                currentActive.classList.remove("active");
                                currentActive.addEventListener(
                                        "animationend",
                                        () => {
                                                currentActive.classList.remove("slide-out-left", "slide-out-right");
                                                nextPanel.classList.remove("slide-in-left", "slide-in-right");
                                        },
                                        { once: true }
                                );

                                prevSelectedRow = row;

                                if (!detailCache.has(id)) {
                                        try {
                                                const data = await fetchDetail(id);
                                                if (nextPanel.dataset.loadedId === String(id)) {
                                                        nextPanel.innerHTML = renderFullDetail(data, rowData, []);
                                                        setupTransferCountdown(nextPanel);
                                                        nextPanel.dataset.matricula = data.matricula || rowData.matricula || "";
                                                        nextPanel.dataset.plan = data.plan || rowData.plan || "";
                                                        syncPdfModalDocs(nextPanel);
                                                }
                                        } catch (e) {
                                                console.error("❌ Error fetch detalle:", e);
                                        } finally {
                                                nextPanel.classList.remove("loading");
                                        }
                                }
                        });

                        tbody.addEventListener("keydown", function (e) {
                                if (e.key !== "Enter") return;
                                const row = e.target.closest(".guarantees-table__row");
                                if (row) row.click();
                        });
                }
		initRowSelection();

                (() => {
                        const modal = document.querySelector(".pdf-modal");
                        if (!modal) return;
                        const iframe = modal.querySelector(".pdf-modal__iframe");
                        const dl = modal.querySelector(".pdf-modal__download");
                        const prevBtn = modal.querySelector(".pdf-modal__nav-btn--prev");
                        const nextBtn = modal.querySelector(".pdf-modal__nav-btn--next");
                        const navContainer = modal.querySelector(".pdf-modal__nav");
                        const spinner = modal.querySelector(".pdf-modal__spinner");
                        const uploadView = modal.querySelector(".pdf-modal__upload");
                        let currentIdx = -1;

                        iframe.addEventListener("load", () => {
                                if (!spinner) {
                                        return;
                                }
                                const currentSrc = iframe.getAttribute("src") || "";
                                if (!currentSrc || currentSrc === "about:blank") {
                                        return;
                                }
                                spinner.classList.remove("active");
                        });

                        function getButtons() {
                                return Array.from(
                                        modal.querySelectorAll(".pdf-modal__docs-list .detail__docs-btn")
                                );
                        }

                        function showUploadView() {
                                if (spinner) spinner.classList.remove("active");
                                if (uploadView) uploadView.hidden = false;
                                if (iframe) {
                                        iframe.hidden = true;
                                        iframe.src = "";
                                }
                                if (dl) {
                                        dl.hidden = true;
                                        dl.removeAttribute("href");
                                }
                                if (navContainer) {
                                        navContainer.hidden = true;
                                }
                                if (prevBtn) {
                                        prevBtn.hidden = true;
                                        prevBtn.disabled = true;
                                }
                                if (nextBtn) {
                                        nextBtn.hidden = true;
                                        nextBtn.disabled = true;
                                }
                        }

                        function showPdfView(buttons) {
                                if (uploadView) uploadView.hidden = true;
                                if (iframe) iframe.hidden = false;
                                if (dl) dl.hidden = false;
                                const docButtons = buttons.filter(
                                        (btn) => btn.dataset.docKey !== ADD_DOC_KEY
                                );
                                const hideNav = docButtons.length <= 1;
                                if (navContainer) navContainer.hidden = hideNav;
                                if (prevBtn) prevBtn.hidden = hideNav;
                                if (nextBtn) nextBtn.hidden = hideNav;
                        }

                        function openDocByIndex(idx) {
                                const buttons = getButtons();
                                if (idx < 0 || idx >= buttons.length) return;
                                const prevIdx = currentIdx;
                                currentIdx = idx;
                                const btn = buttons[idx];
                                const rawUrl = btn.dataset.docUrl || "";
                                const docKey = btn.dataset.docKey || "";
                                const isAddDoc = docKey === ADD_DOC_KEY;
                                buttons.forEach((b, i) => b.classList.toggle("active", i === idx));
                                if (prevBtn) {
                                        prevBtn.disabled = idx === 0;
                                }
                                if (nextBtn) {
                                        nextBtn.disabled = idx === buttons.length - 1;
                                }

                                if (isAddDoc) {
                                        showUploadView();
                                        return;
                                }

                                const url = normalizeDocUrl(rawUrl);
                                if (!url) {
                                        if (spinner) spinner.classList.remove("active");
                                        if (iframe) {
                                                iframe.hidden = false;
                                                iframe.src = "about:blank";
                                        }
                                        return;
                                }

                                showPdfView(buttons);

                if (dl) {
                    const dlUrl = url.includes("?")
                            ? `${url}&download=1`
                            : `${url}?download=1`;
                    dl.href = dlUrl;
                    const downloadText = btn.dataset.docDownload || "Descargar documento";
                    const downloadLabel = dl.querySelector(".pdf-modal__download-text");
                    if (downloadLabel) {
                        downloadLabel.textContent = downloadText;
                    } else {
                        dl.textContent = downloadText;
                    }
                    dl.setAttribute("aria-label", downloadText);
                    if (btn.dataset.docFilename) {
                            dl.download = btn.dataset.docFilename;
                    } else {
                            dl.removeAttribute("download");
                    }
                    dl.hidden = false;
                }

                                const applyIframeSrc = (srcUrl) => {
                                        if (!iframe || currentIdx !== idx) {
                                                return;
                                        }
                                        iframe.hidden = false;
                                        iframe.src = srcUrl;
                                        if (spinner) spinner.classList.remove("active");
                                };

                                const cachedObjectUrl = getCachedDocumentObjectUrl(url);
                                if (cachedObjectUrl) {
                                        if (spinner) spinner.classList.remove("active");
                                        applyIframeSrc(cachedObjectUrl);
                                        return;
                                }

                                if (spinner) spinner.classList.add("active");
                                if (iframe) {
                                        iframe.hidden = false;
                                        iframe.src = "about:blank";
                                }

                                ensureDocumentPreloaded(url)
                                        .then((objectUrl) => {
                                                if (!objectUrl) {
                                                        throw new Error("Documento no disponible");
                                                }
                                                applyIframeSrc(objectUrl);
                                        })
                                        .catch((error) => {
                                                if (currentIdx !== idx) {
                                                        return;
                                                }
                                                console.error(
                                                        "No se pudo cargar el documento",
                                                        error
                                                );
                                                if (spinner) spinner.classList.remove("active");
                                        });
                        }

                        function openDocByKey(key) {
                                if (!key) return;
                                const buttons = getButtons();
                                const idx = buttons.findIndex(
                                        (button) => button.dataset.docKey === key
                                );
                                if (idx !== -1) {
                                        openDocByIndex(idx);
                                }
                        }

                        document.addEventListener("click", (e) => {
                                const btn = e.target.closest(".detail__docs-btn");
                                if (btn) {
                                        const key = btn.dataset.docKey || "";
                                        if (key) {
                                                openDocByKey(key);
                                        } else {
                                                const idx = parseInt(btn.dataset.docIndex || "0", 10);
                                                openDocByIndex(idx);
                                        }
                                        modal.classList.add("visible");
                                }
                        });

                        prevBtn.addEventListener("click", () => openDocByIndex(currentIdx - 1));
                        nextBtn.addEventListener("click", () => openDocByIndex(currentIdx + 1));

                        function closeModal() {
                                modal.classList.remove("visible");
                                iframe.src = "";
                                if (dl) {
                                        dl.href = "#";
                                }
                                currentIdx = -1;
                                if (prevBtn) {
                                        prevBtn.disabled = true;
                                        prevBtn.hidden = false;
                                }
                                if (nextBtn) {
                                        nextBtn.disabled = true;
                                        nextBtn.hidden = false;
                                }
                                if (uploadView) {
                                        uploadView.hidden = true;
                                }
                                if (iframe) {
                                        iframe.hidden = false;
                                }
                                if (navContainer) {
                                        navContainer.hidden = false;
                                }
                                if (dl) {
                                        dl.hidden = false;
                                }
                                if (spinner) spinner.classList.remove("active");
                                getButtons().forEach((b) => b.classList.remove("active"));
                                updateModalControls(modal);
                        }

                        modal.querySelector(".pdf-modal__close").addEventListener("click", closeModal);
                        modal.addEventListener(
                                "click",
                                (e) => e.target === modal && closeModal()
                        );
                        document.addEventListener("keydown", (e) => {
                                if (e.key === "Escape" && modal.classList.contains("visible")) {
                                        closeModal();
                                }
                        });
                })();

                (() => {
                        const filters = document.querySelector(".guarantees-list__filters"),
                                header = document.querySelector(".top-bar");
                        if (filters && header) {
                                new IntersectionObserver(
					([e]) => {
						const a = !e.isIntersecting;
						filters.classList.toggle("sticky-active", a);
						listContainer.classList.toggle("sticky-active", a);
						detail.classList.toggle("sticky-active", a);
					},
					{ root: null, threshold: 0, rootMargin: "-50px" }
				).observe(header);
			}
                        const onScroll = () => {
                                const activePanel = detail.querySelector(".guarantee-detail__panel.active");
                                const detailScrolled = activePanel ? activePanel.scrollTop > 10 : false;
                                document.body.classList.toggle(
                                        "scrolled",
                                        listContainer.scrollTop > 10 || detailScrolled
                                );
                        };
                        listContainer.addEventListener("scroll", onScroll);
                        detail.querySelectorAll(".guarantee-detail__panel").forEach((p) =>
                                p.addEventListener("scroll", onScroll)
                        );
                })();

                function normalizeFilterValues(items) {
                        if (!Array.isArray(items)) {
                                return new Set();
                        }
                        return new Set(
                                items
                                        .map((item) => {
                                                if (typeof item === "string") {
                                                        return item;
                                                }
                                                if (item && typeof item === "object") {
                                                        return item.value || item.slug || "";
                                                }
                                                return "";
                                        })
                                        .filter(Boolean)
                        );
                }

                function syncChannelOptionVisibility(channels = [], vendorTypes = []) {
                        if (!canalSelect) {
                                return;
                        }

                        const channelSet = normalizeFilterValues(channels);
                        const vendorTypeSet = normalizeFilterValues(vendorTypes);
                        let selectionChanged = false;

                        Array.from(canalSelect.options).forEach((option) => {
                                if (option.value === "") {
                                        return;
                                }
                                const channel = option.dataset.channel || option.value || "";
                                const vendorType = option.dataset.vendorType || "";
                                let visible = true;
                                if (vendorType) {
                                        visible = vendorTypeSet.size === 0 || vendorTypeSet.has(vendorType);
                                } else if (channel) {
                                        visible = channelSet.size === 0 || channelSet.has(channel);
                                }
                                option.hidden = !visible;
                        });

                        const selectedOption = canalSelect.options[canalSelect.selectedIndex];
                        if (selectedOption && selectedOption.hidden) {
                                canalSelect.value = "";
                                selectedCanal = "";
                                selectedVendorType = "";
                                selectionChanged = true;
                        }

                        refreshClientsVisibility();

                        if (selectionChanged) {
                                applyFilters();
                        }
                }

                function setOrderSelection(key, { trigger = true } = {}) {
                        if (!orderOptions.has(key)) {
                                return;
                        }
                        const config = orderOptions.get(key) || {};
                        selectedOrderKey = key;
                        selectedOrderBy = config.orderBy || "created";
                        selectedOrderDirection =
                                config.direction === "asc" ? "asc" : "desc";

                        if (orderLabelNode && config.label) {
                                orderLabelNode.textContent = config.label;
                        }

                        if (orderMenu) {
                                orderMenu
                                        .querySelectorAll("[data-sort-key]")
                                        .forEach((button) => {
                                                const isActive =
                                                        button.dataset.sortKey === key;
                                                button.classList.toggle(
                                                        "is-active",
                                                        isActive
                                                );
                                                button.setAttribute(
                                                        "aria-checked",
                                                        isActive ? "true" : "false"
                                                );
                                        });
                        }

                        if (trigger) {
                                applyFilters();
                        }

                        updateResetVisibility();
                }

                function openOrderMenu() {
                        if (!orderRoot || !orderMenu || !orderToggle) {
                                return;
                        }
                        orderRoot.setAttribute("data-open", "true");
                        orderToggle.setAttribute("aria-expanded", "true");
                        orderMenu.removeAttribute("hidden");
                }

                function closeOrderMenu() {
                        if (!orderRoot || !orderMenu || !orderToggle) {
                                return;
                        }
                        orderRoot.removeAttribute("data-open");
                        orderToggle.setAttribute("aria-expanded", "false");
                        orderMenu.setAttribute("hidden", "");
                }

                if (orderMenu) {
                        const buttons = Array.from(
                                orderMenu.querySelectorAll("[data-sort-key]")
                        );
                        buttons.forEach((button) => {
                                const key = button.dataset.sortKey || "";
                                if (!key) {
                                        return;
                                }
                                const orderBy = button.dataset.orderBy || "created";
                                const direction =
                                        (button.dataset.orderDirection || "desc").toLowerCase() ===
                                        "asc"
                                                ? "asc"
                                                : "desc";
                                const label = (button.textContent || "").trim();
                                orderOptions.set(key, {
                                        orderBy,
                                        direction,
                                        label,
                                });
                        });

                        let initialKey = orderToggle
                                ? orderToggle.getAttribute("data-default-sort") || ""
                                : "";
                        if (!initialKey || !orderOptions.has(initialKey)) {
                                const defaultButton = orderMenu.querySelector(
                                        "[data-default][data-sort-key]"
                                );
                                if (
                                        defaultButton &&
                                        orderOptions.has(defaultButton.dataset.sortKey || "")
                                ) {
                                        initialKey = defaultButton.dataset.sortKey;
                                }
                        }
                        if (!initialKey) {
                                const first = orderOptions.keys().next();
                                if (!first.done) {
                                        initialKey = first.value;
                                }
                        }
                        if (initialKey) {
                                setOrderSelection(initialKey, { trigger: false });
                        }

                        buttons.forEach((button) => {
                                button.addEventListener("click", () => {
                                        const key = button.dataset.sortKey || "";
                                        if (!key) {
                                                return;
                                        }
                                        closeOrderMenu();
                                        setOrderSelection(key);
                                });
                        });
                }

                if (orderToggle && orderMenu) {
                        orderToggle.addEventListener("click", (event) => {
                                event.preventDefault();
                                const isOpen = orderRoot?.getAttribute("data-open") === "true";
                                if (isOpen) {
                                        closeOrderMenu();
                                } else {
                                        openOrderMenu();
                                }
                        });
                }

                if (orderRoot && orderMenu) {
                        document.addEventListener("click", (event) => {
                                if (orderMenu.hasAttribute("hidden")) {
                                        return;
                                }
                                if (orderRoot.contains(event.target)) {
                                        return;
                                }
                                closeOrderMenu();
                        });

                        document.addEventListener("keydown", (event) => {
                                if (event.key !== "Escape") {
                                        return;
                                }
                                if (orderMenu.hasAttribute("hidden")) {
                                        return;
                                }
                                closeOrderMenu();
                                if (orderToggle) {
                                        orderToggle.focus();
                                }
                        });
                }

                async function fetchFilters() {
                        try {
                                const res = await fetch(
                                        `${restRoot}go/v1/guarantees/filters`,{headers:{"X-WP-Nonce":restNonce}});if(!res.ok)throw res.status;const{estados=[],planes=[],concesionarios=[],payment_methods:paymentMethods=[],channels=[],vendor_types:vendorTypes=[],commercials=[],}=await res.json();if(estadoSelect){estadoSelect.querySelectorAll("option:not(:first-child)").forEach((o)=>o.remove());estados.forEach((est)=>{const opt=document.createElement("option");const val=typeof est==="object"?est.value:est;const lbl=typeof est==="object"?est.label:est;if(isProfesional&&["expira_pronto","pendiente_cobro",].includes(String(val))){return;}
opt.value=val;opt.textContent=lbl;estadoSelect.appendChild(opt);});}
if(planSelect){planSelect.querySelectorAll("option:not(:first-child)").forEach((o)=>o.remove());planes.forEach((pl)=>{const opt=document.createElement("option");opt.value=pl.id;opt.textContent=pl.title;planSelect.appendChild(opt);});}
if(Array.isArray(concesionarios)){rawClients=concesionarios.map((client)=>({id:client.id,name:client.name,type:client.type||"",}));populateClientsSelect(selectedVendorType);}
if(Array.isArray(commercials)){rawCommercials=commercials.map((commercial)=>{const rawId=commercial&&typeof commercial.id!=="undefined"?commercial.id:commercial&&typeof commercial.ID!=="undefined"?commercial.ID:0;const id=Number.parseInt(rawId,10);const primaryName=commercial&&typeof commercial.name==="string"?commercial.name:"";const fallbackName=commercial&&typeof commercial.display_name==="string"?commercial.display_name:"";return{id:Number.isFinite(id)?id:0,name:primaryName||fallbackName,};}).filter((item)=>item.id>0&&item.name);}else{rawCommercials=[];}
populateCommercialSelect();if(paymentSelect){paymentSelect.querySelectorAll("option:not(:first-child)").forEach((o)=>o.remove());paymentMethods.forEach((method)=>{const opt=document.createElement("option");if(method&&typeof method==="object"){opt.value=method.value;opt.textContent=method.label;}else{const value=typeof method==="string"?method:"";opt.value=value;opt.textContent=value;}
paymentSelect.appendChild(opt);});}
syncChannelOptionVisibility(channels,vendorTypes);refreshClientsVisibility();updateResetVisibility();if(advancedPanel&&!advancedPanel.hasAttribute("hidden")){requestAnimationFrame(updateAdvancedHeight);}}catch(e){console.error("❌ Error fetching filters:",e);}}
function applyFilters(){currentPage=1;hasMore=true;lastValidQuery="";lastValidResults=[];const cacheKey=buildListCacheKey(searchQuery,selectedEstado,selectedPlan,selectedCanal,selectedConcesionario,selectedVendorType,selectedPaymentMethod,selectedOrderBy,selectedOrderDirection,selectedCommercial);tbody.innerHTML="";loadedIds.clear();clearSelectionAndDetail();spinner.style.display="";if(currentListAbort)currentListAbort.abort();isLoading=false;if(listCache.has(cacheKey)){renderFromCache(listCache.get(cacheKey));return;}
loadPage(1);}
if(estadoSelect)
estadoSelect.addEventListener("change",()=>{selectedEstado=estadoSelect.value;applyFilters();updateResetVisibility();});if(planSelect)
planSelect.addEventListener("change",()=>{selectedPlan=planSelect.value;applyFilters();updateResetVisibility();});if(paymentSelect)
paymentSelect.addEventListener("change",()=>{selectedPaymentMethod=paymentSelect.value;applyFilters();updateResetVisibility();});if(canalSelect)
canalSelect.addEventListener("change",()=>{refreshClientsVisibility();applyFilters();updateResetVisibility();});if(concesionarioSelect)
concesionarioSelect.addEventListener("change",()=>{selectedConcesionario=concesionarioSelect.value;applyFilters();updateResetVisibility();});if(commercialSelect)
commercialSelect.addEventListener("change",()=>{selectedCommercial=commercialSelect.value;applyFilters();updateResetVisibility();});if(moreFiltersToggle&&advancedPanel){const labelNode=moreFiltersToggle.querySelector(".guarantees-list__more-filters-label");const defaultLabel=moreFiltersToggle.getAttribute("data-default-label")||(labelNode?labelNode.textContent||"":"");const activeLabel=moreFiltersToggle.getAttribute("data-active-label")||defaultLabel;setAdvancedOpen=(open)=>{if(!advancedPanel)return;if(open){advancedPanel.removeAttribute("hidden");}else{advancedPanel.setAttribute("hidden","");}
moreFiltersToggle.setAttribute("aria-expanded",open?"true":"false");moreFiltersToggle.setAttribute("data-open",open?"true":"false");if(filtersRoot){if(open){filtersRoot.setAttribute("data-advanced-open","true");}else{filtersRoot.removeAttribute("data-advanced-open");}}
if(labelNode){labelNode.textContent=open?activeLabel:defaultLabel;}
requestAnimationFrame(updateAdvancedHeight);};moreFiltersToggle.addEventListener("click",()=>{const isOpen=!advancedPanel.hasAttribute("hidden");setAdvancedOpen(!isOpen);});}
refreshClientsVisibility();updateAdvancedHeight();fetchFilters();const input=document.getElementById("buscador_mis_garantias");const closeIcon=document.querySelector(".guarantees-list__close-icon");let debounceTimer=null;const DEBOUNCE_MS=300;if(resetFiltersBtn){resetFiltersBtn.addEventListener("click",()=>{if(input){input.value="";}
if(closeIcon){closeIcon.classList.remove("visible");}
searchQuery="";lastValidQuery="";lastValidResults=[];if(estadoSelect)estadoSelect.value="";if(planSelect)planSelect.value="";if(paymentSelect)paymentSelect.value="";if(canalSelect)canalSelect.value="";if(concesionarioSelect)concesionarioSelect.value="";if(commercialSelect)commercialSelect.value="";selectedEstado="";selectedPlan="";selectedCanal="";selectedConcesionario="";selectedVendorType="";selectedPaymentMethod="";selectedCommercial="";currentPage=1;hasMore=true;refreshClientsVisibility();if(orderToggle){const defaultSort=orderToggle.getAttribute("data-default-sort")||"created_desc";if(defaultSort){setOrderSelection(defaultSort,{trigger:false});}}else{selectedOrderKey="created_desc";selectedOrderBy="created";selectedOrderDirection="desc";}
setResultMessage("");applyFilters();if(advancedPanel&&!advancedPanel.hasAttribute("hidden")){requestAnimationFrame(updateAdvancedHeight);}else{updateBaseFiltersHeight();rootElement.style.setProperty("--go-advanced-filters-height","0px");}
updateResetVisibility();});}
function doSearch(query){searchQuery=query;currentPage=1;hasMore=true;const cacheKey=buildListCacheKey(searchQuery,selectedEstado,selectedPlan,selectedCanal,selectedConcesionario,selectedVendorType,selectedPaymentMethod,selectedOrderBy,selectedOrderDirection,selectedCommercial);tbody.innerHTML="";loadedIds.clear();clearSelectionAndDetail();spinner.style.display="";if(currentListAbort)currentListAbort.abort();isLoading=false;if(listCache.has(cacheKey)){renderFromCache(listCache.get(cacheKey));return;}
loadPage(1,{search:searchQuery});updateResetVisibility();}
input.addEventListener("input",()=>{const value=input.value.trim();if(value.length>0){closeIcon.classList.add("visible");}else{closeIcon.classList.remove("visible");lastValidQuery="";lastValidResults=[];}
searchQuery=value;updateResetVisibility();if(debounceTimer)clearTimeout(debounceTimer);debounceTimer=setTimeout(()=>{doSearch(value);},DEBOUNCE_MS);});closeIcon.addEventListener("click",()=>{input.value="";closeIcon.classList.remove("visible");input.focus();input.select();if(debounceTimer)clearTimeout(debounceTimer);searchQuery="";currentPage=1;hasMore=true;lastValidQuery="";lastValidResults=[];loadPage(1);updateResetVisibility();});new IntersectionObserver((entries)=>{if(entries[0].isIntersecting&&hasMore&&!isLoading){loadPage(currentPage+1);}},{root:listContainer,threshold:0.1,rootMargin:"200px 0px"}).observe(scrollEnd);const initialLoadPromise=loadPage(1);if(initialMatQuery){initialLoadPromise.catch(()=>{}).finally(()=>{if(initialMatQuery){preloadByPlate(initialMatQuery);}});}});})();